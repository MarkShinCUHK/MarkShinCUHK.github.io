2:I["8c0f216c4604",[],"Children",1]
3:I["593f344dc510",[],"GlobalErrorBoundary",1]
4:I["0b874ad30386",[],"default",1]
5:I["593f344dc510",[],"ErrorBoundary",1]
6:I["15c18cfaeeff",[],"LayoutSegmentProvider",1]
7:I["8c0f216c4604",[],"Slot",1]
8:I["9276801271d6",[],"AppRouterScrollTarget",1]
9:I["593f344dc510",[],"RedirectBoundary",1]
:HL["/hdat-ds-study-hub/_next/static/css/index._5LBKVvE.css","style"     ]
0:{"__route":"route:/templates/07","__interceptionContext":null,"__layoutIds":["layout:/"],"__rootLayout":"/","__sourcePage":"/templates/[chapter]/page","page:/templates/07":"$L1","layout:/":[[[["$","link","css:/hdat-ds-study-hub/_next/static/css/index._5LBKVvE.css",{"rel":"stylesheet","precedence":"vite-rsc/importer-resources","href":"/hdat-ds-study-hub/_next/static/css/index._5LBKVvE.css","data-rsc-css-href":"/hdat-ds-study-hub/_next/static/css/index._5LBKVvE.css"}],"$undefined"],["$","html",null,{"lang":"ko","children":[["$","head",null,{"children":[["$","meta",null,{"name":"theme-color","content":"#07152b"}],["$","meta",null,{"name":"description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"name":"application-name","content":"HDAT DS Study Hub"}],["$","meta",null,{"name":"author","content":"HDAT DS Study Hub"}],["$","meta",null,{"name":"robots","content":"index, follow"}],["$","meta",null,{"property":"og:type","content":"website"}],["$","meta",null,{"property":"og:locale","content":"ko_KR"}],["$","meta",null,{"property":"og:url","content":"https://markshincuhk.github.io/hdat-ds-study-hub"}],["$","meta",null,{"property":"og:site_name","content":"HDAT DS Study Hub"}],["$","meta",null,{"property":"og:title","content":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}],["$","meta",null,{"property":"og:description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"property":"og:image","content":"https://markshincuhk.github.io/hdat-ds-study-hub/og.png"}],["$","meta",null,{"property":"og:image:width","content":"1726"}],["$","meta",null,{"property":"og:image:height","content":"911"}],["$","meta",null,{"property":"og:image:alt","content":"Python 기초부터 HDAT DS까지 배우는 PyTorch 학습실"}],["$","meta",null,{"name":"twitter:card","content":"summary_large_image"}],["$","meta",null,{"name":"twitter:title","content":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}],["$","meta",null,{"name":"twitter:description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"name":"twitter:image","content":"https://markshincuhk.github.io/hdat-ds-study-hub/og.png"}],["$","link",null,{"rel":"icon","href":"/hdat-ds-study-hub/favicon.svg","type":"image/svg+xml"}],["$","title",null,{"children":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}]]}],["$","body",null,{"children":["$","$L2",null,{}]}]]}]],null],"route:/templates/07":[[["$","meta",null,{"charSet":"utf-8"}],[["$","title","0",{"children":"입력·전처리·분할·저장 함수 사전 | HDAT DS"}]],[["$","meta","0",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]],["$","$L3",null,{"fallback":"$4","children":["$","$L5",null,{"fallback":"$4","children":["$","$L6",null,{"providerId":"layout:/","segmentMap":{"children":["templates","07"]},"children":["$","$L7",null,{"id":"layout:/","parallelSlots":"$undefined","children":["$","$L8",null,{"children":["$","$L9",null,{"children":[["$","$L6",null,{"providerId":"page:/templates/07","segmentMap":{"children":["__PAGE__"]},"children":["$","$L7",null,{"id":"page:/templates/07"}]}],null]}]}]}]}]}]}],null,null],"__layoutFlags":{"layout:/":"s"},"__artifactCompatibility":{"schemaVersion":1,"graphVersion":"app-route-graph:467b84659031f790","deploymentVersion":"b64f7457-4db4-4eee-85a6-5a7ef05d9edc","appElementsSchemaVersion":1,"rscPayloadSchemaVersion":1,"rootBoundaryId":"/","renderEpoch":null},"__renderObservation":{"schemaVersion":1,"output":{"kind":"app-rsc","mountedSlotsFingerprint":null,"renderEpoch":null,"rootBoundaryId":"/","routeId":"route:/templates/07"},"completeness":"partial","boundaryOutcome":{"kind":"unknown"},"requestApis":[{"kind":"connection","status":"unknown"},{"kind":"cookies","status":"unknown"},{"kind":"draftMode","status":"unknown"},{"kind":"headers","status":"unknown"},{"kind":"params","status":"observed"},{"kind":"searchParams","status":"unknown"}],"dynamicFetches":[],"cacheTags":["/templates/07","_N_T_/layout","_N_T_/templates/07","_N_T_/templates/[chapter]/layout","_N_T_/templates/[chapter]/page","_N_T_/templates/layout"],"pathTags":["/templates/07"],"cacheability":"unknown","downgrade":{"target":"freshRender","reasons":[{"code":"CP_DOWNGRADE_CACHEABILITY_UNKNOWN","target":"freshRender"},{"code":"CP_DOWNGRADE_INCOMPLETE_OBSERVATION","completeness":"partial","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"connection","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"cookies","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"draftMode","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"headers","target":"freshRender"},{"code":"CP_DOWNGRADE_PUBLIC_REQUEST_API","requestApi":"params","target":"publicVariant"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"searchParams","target":"freshRender"}],"fallback":{"kind":"breakerFallback","code":"CP_PRIVATE_DYNAMIC_DOWNGRADE","mode":"renderFresh","scope":"affectedOutput","fields":{"reasonCodes":["CP_DOWNGRADE_CACHEABILITY_UNKNOWN","CP_DOWNGRADE_INCOMPLETE_OBSERVATION","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_PUBLIC_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API"],"target":"freshRender"}},"isPublicCacheCandidate":false}}}
a:I["326fe58ccdc4",[],"default",1]
1:[["$","$La",null,{"active":"playbook"}],["$","div",null,{"className":"playbook-layout shell-wide","children":[["$","aside",null,{"className":"playbook-sidebar","children":[["$","a",null,{"className":"sidebar-back","href":"/hdat-ds-study-hub/templates/","children":"← 템플릿 호출 매뉴얼"}],["$","nav",null,{"aria-label":"호출 매뉴얼 장 목록","children":[["$","a","01",{"href":"/hdat-ds-study-hub/templates/01/","aria-current":"$undefined","children":[["$","span",null,{"children":"01"}],"함수의 괄호를 읽는 법부터"]}],["$","a","02",{"href":"/hdat-ds-study-hub/templates/02/","aria-current":"$undefined","children":[["$","span",null,{"children":"02"}],"표 데이터 하나로 처음부터 끝까지 호출하기"]}],["$","a","03",{"href":"/hdat-ds-study-hub/templates/03/","aria-current":"$undefined","children":[["$","span",null,{"children":"03"}],"회귀를 분류로 바꿀 때 함께 바꿀 것"]}],["$","a","04",{"href":"/hdat-ds-study-hub/templates/04/","aria-current":"$undefined","children":[["$","span",null,{"children":"04"}],"시계열에서 T와 F를 실제 값으로 바꾸기"]}],["$","a","05",{"href":"/hdat-ds-study-hub/templates/05/","aria-current":"$undefined","children":[["$","span",null,{"children":"05"}],"이미지와 오토인코더는 어디가 다른가요?"]}],["$","a","06",{"href":"/hdat-ds-study-hub/templates/06/","aria-current":"$undefined","children":[["$","span",null,{"children":"06"}],"지표·손실·저장 인자를 내 문제에 맞추기"]}],["$","a","07",{"href":"/hdat-ds-study-hub/templates/07/","aria-current":"page","children":[["$","span",null,{"children":"07"}],"입력·전처리·분할·저장 함수 사전"]}],["$","a","08",{"href":"/hdat-ds-study-hub/templates/08/","aria-current":"$undefined","children":[["$","span",null,{"children":"08"}],"모델을 만드는 인자 사전"]}],["$","a","09",{"href":"/hdat-ds-study-hub/templates/09/","aria-current":"$undefined","children":[["$","span",null,{"children":"09"}],"loader·학습·예측의 모든 인자"]}]]}]]}],["$","main",null,{"id":"main-content","className":"playbook-article","data-playbook":true,"children":[["$","header",null,{"className":"lesson-header","children":[["$","p",null,{"className":"micro-label","children":["hdat_templates · 호출 매뉴얼 ","07"]}],["$","h1",null,{"children":"입력·전처리·분할·저장 함수 사전"}],["$","p",null,{"className":"playbook-note","children":"정의 → 값의 출처 → 호출 → 반환값 → 다음 단계"}]]}],["$","details",null,{"className":"playbook-mobile-list","children":[["$","summary",null,{"children":"매뉴얼의 다른 장 고르기"}],["$","nav",null,{"children":[["$","a","01",{"href":"/hdat-ds-study-hub/templates/01/","aria-current":"$undefined","children":["01",". ","함수의 괄호를 읽는 법부터"]}],["$","a","02",{"href":"/hdat-ds-study-hub/templates/02/","aria-current":"$undefined","children":["02",". ","표 데이터 하나로 처음부터 끝까지 호출하기"]}],["$","a","03",{"href":"/hdat-ds-study-hub/templates/03/","aria-current":"$undefined","children":["03",". ","회귀를 분류로 바꿀 때 함께 바꿀 것"]}],["$","a","04",{"href":"/hdat-ds-study-hub/templates/04/","aria-current":"$undefined","children":["04",". ","시계열에서 T와 F를 실제 값으로 바꾸기"]}],["$","a","05",{"href":"/hdat-ds-study-hub/templates/05/","aria-current":"$undefined","children":["05",". ","이미지와 오토인코더는 어디가 다른가요?"]}],["$","a","06",{"href":"/hdat-ds-study-hub/templates/06/","aria-current":"$undefined","children":["06",". ","지표·손실·저장 인자를 내 문제에 맞추기"]}],["$","a","07",{"href":"/hdat-ds-study-hub/templates/07/","aria-current":"page","children":["07",". ","입력·전처리·분할·저장 함수 사전"]}],["$","a","08",{"href":"/hdat-ds-study-hub/templates/08/","aria-current":"$undefined","children":["08",". ","모델을 만드는 인자 사전"]}],["$","a","09",{"href":"/hdat-ds-study-hub/templates/09/","aria-current":"$undefined","children":["09",". ","loader·학습·예측의 모든 인자"]}]]}]]}],["$","nav",null,{"className":"playbook-section-toc","aria-label":"현재 장 목차","children":[["$","a","api-LazyWindowDataset",{"href":"#api-LazyWindowDataset","children":"LazyWindowDataset"}],["$","a","api-TabularConfig",{"href":"#api-TabularConfig","children":"TabularConfig"}],["$","a","api-seed_everything",{"href":"#api-seed_everything","children":"seed_everything"}],["$","a","api-quick_audit",{"href":"#api-quick_audit","children":"quick_audit"}],["$","a","api-assert_frame_contract",{"href":"#api-assert_frame_contract","children":"assert_frame_contract"}],["$","a","api-minmax_selected",{"href":"#api-minmax_selected","children":"minmax_selected"}],["$","a","api-fit_iqr_bounds",{"href":"#api-fit_iqr_bounds","children":"fit_iqr_bounds"}],["$","a","api-apply_clip_bounds",{"href":"#api-apply_clip_bounds","children":"apply_clip_bounds"}],["$","a","api-add_datetime_features",{"href":"#api-add_datetime_features","children":"add_datetime_features"}],["$","a","api-clean_tabular_values",{"href":"#api-clean_tabular_values","children":"clean_tabular_values"}],["$","a","api-crop_to_numpy",{"href":"#api-crop_to_numpy","children":"crop_to_numpy"}],"$Lb","$Lc","$Ld","$Le","$Lf","$L10","$L11","$L12","$L13","$L14","$L15","$L16","$L17","$L18","$L19","$L1a","$L1b","$L1c"]}],"$L1d","$L1e",false,"$L1f","$L20","$L21"]}]]}],"$L22"]
23:I["66e71be26559",[],"default",1]
24:I["ad205a1e87e8",[],"default",1]
b:["$","a","api-conv_output_size",{"href":"#api-conv_output_size","children":"conv_output_size"}]
c:["$","a","api-safe_train_valid_indices",{"href":"#api-safe_train_valid_indices","children":"safe_train_valid_indices"}]
d:["$","a","api-make_preprocessor",{"href":"#api-make_preprocessor","children":"make_preprocessor"}]
e:["$","a","api-make_tabular_estimator",{"href":"#api-make_tabular_estimator","children":"make_tabular_estimator"}]
f:["$","a","api-evaluate_predictions",{"href":"#api-evaluate_predictions","children":"evaluate_predictions"}]
10:["$","a","api-assert_class_fold_coverage",{"href":"#api-assert_class_fold_coverage","children":"assert_class_fold_coverage"}]
11:["$","a","api-fit_tabular_baseline",{"href":"#api-fit_tabular_baseline","children":"fit_tabular_baseline"}]
12:["$","a","api-predict_tabular_probabilities",{"href":"#api-predict_tabular_probabilities","children":"predict_tabular_probabilities"}]
13:["$","a","api-make_point_forecast_windows",{"href":"#api-make_point_forecast_windows","children":"make_point_forecast_windows"}]
14:["$","a","api-make_grouped_point_forecast_windows",{"href":"#api-make_grouped_point_forecast_windows","children":"make_grouped_point_forecast_windows"}]
15:["$","a","api-split_raw_time_then_window",{"href":"#api-split_raw_time_then_window","children":"split_raw_time_then_window"}]
16:["$","a","api-fit_scale_3d",{"href":"#api-fit_scale_3d","children":"fit_scale_3d"}]
17:["$","a","api-transform_scale_3d",{"href":"#api-transform_scale_3d","children":"transform_scale_3d"}]
18:["$","a","api-validate_prediction_array",{"href":"#api-validate_prediction_array","children":"validate_prediction_array"}]
19:["$","a","api-save_npy_submission",{"href":"#api-save_npy_submission","children":"save_npy_submission"}]
1a:["$","a","api-save_csv_submission",{"href":"#api-save_csv_submission","children":"save_csv_submission"}]
1b:["$","a","api-prepare_numpy_images",{"href":"#api-prepare_numpy_images","children":"prepare_numpy_images"}]
1c:["$","a","api-print_shapes",{"href":"#api-print_shapes","children":"print_shapes"}]
1d:["$","$L23",null,{}]
1e:["$","$L24",null,{"recipe":"template-07"}]
25:T17b94,<p>열 이름, 배열, 분할 위치, 설정 객체를 구별하는 사전입니다. 각 예제는 해당 항목의 준비 코드부터 실행하세요. TabularConfig와 sklearn 모델 보조 함수는 기존 파일의 API를 빠짐없이 설명하기 위한 참고이며, 앞의 실전 학습 예제는 모두 PyTorch입니다.</p>
<h4 id="api-LazyWindowDataset"><code>LazyWindowDataset</code></h4>
<p>모든 윈도를 한 번에 복제하지 않고, 요청한 시작 위치에서 한 윈도씩 꺼낸다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>features</code></td>
<td>필수</td>
<td><code>series_df[['speed', 'temperature']].to_numpy(dtype=np.float32)</code></td>
<td>정답·ID를 제외한 시간순 원시 특성 행. (T,F), 1D (T,)도 한 특성으로 확장 가능. 시간 축 T는 전체 원시 행 수. 그룹 함수에서는 원래 행 대응 유지.</td>
</tr>
<tr>
<td><code>targets</code></td>
<td><code>None</code></td>
<td><code>series_df[['fuel_use']].to_numpy(dtype=np.float32)</code></td>
<td>각 원시 행 시점의 정답 값 배열. (T,) 또는 (T,D), features와 같은 원시 행 길이. 미래 정답을 helper가 인덱스로 고르므로 이미 앞당긴 y를 다시 horizon만큼 이동하지 않는다.</td>
</tr>
<tr>
<td><code>lookback</code></td>
<td><code>20</code></td>
<td><code>20</code></td>
<td>문제에서 사용할 과거 시간 길이를 샘플 개수로 환산. 양의 정수. 100Hz에서 0.2초를 20개로 해석하는 문제 관례. 초 0.2를 그대로 넣지 않는다.</td>
</tr>
<tr>
<td><code>horizon</code></td>
<td><code>1</code></td>
<td><code>40</code></td>
<td>마지막 입력 관측에서 목표까지의 간격을 행 개수로 환산. 0 이상 정수. 100Hz에서 0.4초 뒤는 40칸. 입력 시작이 아니라 마지막 입력에서 센다.</td>
</tr>
<tr>
<td><code>stride</code></td>
<td><code>1</code></td>
<td><code>1</code></td>
<td>윈도 시작 위치를 몇 행씩 이동할지 정한 값. 양의 정수. 윈도 helper에서는 초 단위가 아닌 시간순 행 간격.</td>
</tr>
<tr>
<td><code>starts</code></td>
<td><code>None</code></td>
<td><code>np.array([0, 5, 10])</code></td>
<td>제출 행 또는 선택한 학습 표본에 대응하도록 계산한 입력 시작 행 위치. 1D 정수 위치. targets=None이면 필수. end_idx−lookback+1 또는 target_idx−horizon−lookback+1로 계산. 함수가 제출 순서를 추론하지 않는다.</td>
</tr>
<tr>
<td><code>expected_n</code></td>
<td><code>None</code></td>
<td><code>len(submission_rows)</code></td>
<td>제출 명세/선택한 표본 목록에서 정한 예상 윈도 수. 정수 또는 None. None이면 행 수 검증 생략. 원시 행 수 T와 윈도 수 N은 다르다.</td>
</tr>
<tr>
<td><code>assume_sorted</code></td>
<td><code>False</code></td>
<td><code>True</code></td>
<td>실제로 안정 시간순 정렬·행 대응을 확인한 뒤 명시하는 확인값. bool. True가 정렬을 실행해 주는 것은 아니다. 기본 False인 일반 윈도/분할/Lazy 호출은 오류를 낸다.</td>
</tr>
</tbody></table></div>
<p><strong><strong>len</strong> 호출 인자</strong></p>
<p>직접 전달할 인자는 없다.</p>
<p>반환: 윈도 개수 int</p>
<p><strong><strong>getitem</strong> 호출 인자</strong></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>idx</code></td>
<td>필수</td>
<td><code>0</code></td>
<td>Dataset 안에서 꺼낼 윈도의 순서. Python 정수 위치 0..len(ds)-1. 원시 start 위치가 아니라 starts 배열의 인덱스.</td>
</tr>
</tbody></table></div>
<p>반환: (x,y) 또는 x; LazyWindowDataset 반환 계약 참조.</p>
<p><strong>반환값</strong></p>
<p>Dataset 객체. len(ds)=윈도 수. ds[i]는 targets가 있으면 (x:(L,F), y:(D,)), 없으면 x:(L,F) Tensor.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-1">복사</button></div><pre id="template-code-07-1" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 1"><code class="language-python">import hdat_templates as h
import numpy as np
X = np.arange(24, dtype=np.float32).reshape(12, 2)
y = np.arange(12, dtype=np.float32)
train_ds = h.LazyWindowDataset(X, y, lookback=3, horizon=2,
                             assume_sorted=True)
xb, yb = train_ds[0]  # X[0:3], y[4]
assert xb.shape == (3, 2) and yb.tolist() == [4.0]
# 여기서는 제출할 세 행이 입력 끝 2, 7, 11에 대응한다고 정했다.
end_indices = np.array([2, 7, 11])
starts = end_indices - 3 + 1
test_ds = h.LazyWindowDataset(X, targets=None, lookback=3,
                            starts=starts, expected_n=3, assume_sorted=True)
assert len(test_ds) == 3
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>원시 X/y는 CPU float32 Tensor로 보관한다. 전체 윈도 복제를 피하지만 원시 데이터 자체는 메모리에 필요하다.</li>
<li>targets=None이면 starts 필수: 제출 각 행과 일치하는 입력 시작 위치를 명시한다. expected_n도 권장한다.</li>
<li>horizon은 targets가 있을 때 y 선택에 사용. 추론에서는 입력이 실제 관측 범위에 있는지를 검사하며 미래 y 공간을 요구하지 않는다.</li>
<li>그룹 경계 검사가 없으므로 여러 차량을 이어 붙인 원시 배열에 무수정 적용하지 않는다.</li>
<li>starts는 전달한 순서를 그대로 쓰며 int 변환 과정에서 소수가 잘릴 수 있으므로 호출자가 정수·중복·행 의미를 먼저 검사한다.</li>
<li>회귀 y는 적합하지만 다중분류 labels도 float32로 바뀐다. 공통 루프는 hard 정수 값이면 long으로 바꾸지만 class mapping은 별도다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-TabularConfig"><code>TabularConfig</code></h4>
<p>표형 sklearn baseline 흐름에 전달할 선택값을 한 객체에 묶는다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>task</code></td>
<td><code>'regression'</code></td>
<td><code>'binary'</code></td>
<td>정답이 연속값인지, 이진/다중분류/다중라벨인지 문제에서 판별. 'regression'|'binary'|'multiclass'|'multilabel'의 정확한 문자열. 출력·정답·loss 계약이 함께 바뀐다. 모든 함수가 오타를 즉시 검사하는 것은 아니다.</td>
</tr>
<tr>
<td><code>target_cols</code></td>
<td><code>('target',)</code></td>
<td><code>('fault',)</code></td>
<td>제공된 훈련 표의 실제 정답 열 이름과 출력 순서. tuple[str,...]. 기본 ('target',)은 예시이며 실제 열 이름으로 수정해야 한다.</td>
</tr>
<tr>
<td><code>drop_cols</code></td>
<td><code>()</code></td>
<td><code>('vehicle_id', 'record_id')</code></td>
<td>정답 이외의 ID·누수 열 등 실제로 제외할 열. tuple[str,...]. target_cols/datetime_cols와 겹치면 wrapper가 거절. group_col을 지정해도 자동으로 빠지지 않는다.</td>
</tr>
<tr>
<td><code>datetime_cols</code></td>
<td><code>()</code></td>
<td><code>('recorded_at',)</code></td>
<td>날짜 파생 특성을 만들 실제 원본 열. tuple[str,...]. train/test에 있어야 한다. 파싱 실패는 NaT, 파생 열은 NaN으로 처리된다.</td>
</tr>
<tr>
<td><code>time_col</code></td>
<td><code>None</code></td>
<td><code>'recorded_at'</code></td>
<td>시간순 검증에 쓸 훈련 표의 실제 열 이름. 문자열 또는 None. 그룹 분할용 group_col과 동시에 설정하지 않는다. 이 설정 객체에는 time_values 필드가 없다.</td>
</tr>
<tr>
<td><code>group_col</code></td>
<td><code>None</code></td>
<td><code>'vehicle_id'</code></td>
<td>차량·사람 등 같은 집단을 묶을 실제 열 이름. 문자열 또는 None. split 전에 원래 X에서 값을 확보. 테스트가 새 차량이면 적합한 후보이며 ID 자동 삭제 기능은 없다.</td>
</tr>
<tr>
<td><code>metric</code></td>
<td><code>'rmse'</code></td>
<td><code>'f1_macro'</code></td>
<td>문제에서 정한 공식 평가 지표 이름. mse|rmse|mae|rmsle|accuracy|f1|f1_macro|auc|roc_auc. f1도 이 helper에서는 macro이며 양성 F1 별칭이 아니다.</td>
</tr>
<tr>
<td><code>model_name</code></td>
<td><code>'extra_trees'</code></td>
<td><code>'linear'</code></td>
<td>문제 명세 또는 검증 예산으로 고른 sklearn 기준 모델 종류. 'linear'|'extra_trees'. 'MLP'나 이미 생성된 객체가 아니다.</td>
</tr>
<tr>
<td><code>encoding</code></td>
<td><code>'onehot'</code></td>
<td><code>'ordinal'</code></td>
<td>범주 열의 고유값 수·메모리와 모델에 맞춰 정한 표현. 'onehot'|'ordinal'만 사용. 현 구현은 onehot 이외 문자열을 ordinal로 처리하므로 오타 주의.</td>
</tr>
<tr>
<td><code>valid_size</code></td>
<td><code>0.2</code></td>
<td><code>0.2</code></td>
<td>검증에 남길 비율: 문제 조건 또는 사전에 고정한 계획. 0&lt;값&lt;1. 무작위/시간은 행 기준, 그룹 분할에서는 고유 그룹 기준 비율.</td>
</tr>
<tr>
<td><code>seed</code></td>
<td><code>42</code></td>
<td><code>42</code></td>
<td>본인이 고정한 실험 재현용 정수. 문제에 지정값이 있으면 그것을 사용. 정수 하나. 같은 seed가 모든 GPU 연산의 완전한 결정론을 보장하지 않는다.</td>
</tr>
<tr>
<td><code>output_kind</code></td>
<td><code>None</code></td>
<td><code>'label'</code></td>
<td>점수 이름이 아니라 제출 파일이 요구하는 값의 종류. 회귀 value, 분류 label/probability. None이면 일반적으로 회귀 value·분류 label; AUC에서는 명시해야 한다.</td>
</tr>
<tr>
<td><code>positive_label</code></td>
<td><code>1</code></td>
<td><code>'fault'</code></td>
<td>이진 확률 열이 의미해야 할 원래 정답 라벨. 학습된 classes_ 안의 값. 제출 확률 열 선택에 쓰며 훈련 라벨을 자동 0/1 매핑하지 않는다.</td>
</tr>
<tr>
<td><code>class_order</code></td>
<td><code>None</code></td>
<td><code>('normal', 'minor', 'major')</code></td>
<td>제출 확률 열의 정해진 클래스 순서. 모든 학습 클래스를 중복 없이 포함한 순서. None은 estimator classes_ 순서.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>TabularConfig 인스턴스. cfg.task, cfg.target_cols처럼 속성으로 읽는다.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-2">복사</button></div><pre id="template-code-07-2" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 2"><code class="language-python">import hdat_templates as h
cfg = h.TabularConfig(
    task="binary",
    target_cols=("fault",),       # 제공된 훈련 표의 실제 정답 열
    drop_cols=("record_id", "vehicle_id"),
    group_col="vehicle_id",       # 새 차량을 검증 대상으로 분리
    metric="f1_macro",
    model_name="extra_trees",
    encoding="ordinal",
    valid_size=0.2,
    output_kind="label",          # 제출은 확률이 아니라 0/1 라벨
)
assert cfg.target_cols == ("fault",)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>기술적으로 모든 필드에 기본값이 있지만 실제 문제의 정답 열·task·metric·출력 종류는 반드시 대조한다.</li>
<li>target_cols=('target',)의 'target'은 실제 열 이름을 대신하는 기본 예시다. task를 binary로 바꿔도 metric='rmse'는 자동 변경되지 않는다.</li>
<li>time_col과 group_col을 동시에 설정하면 wrapper의 split 단계가 거절한다.</li>
<li>group_col은 분할 용도일 뿐 자동 drop하지 않는다. 새 차량 ID를 빼려면 drop_cols에 명시한다.</li>
<li>PyTorch train_torch_model은 이 객체를 받지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-seed_everything"><code>seed_everything</code></h4>
<p>Python·NumPy·가능한 PyTorch의 난수 시작점을 맞춘다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>seed</code></td>
<td><code>42</code></td>
<td><code>42</code></td>
<td>본인이 고정한 실험 재현용 정수. 문제에 지정값이 있으면 그것을 사용. 정수 하나. 같은 seed가 모든 GPU 연산의 완전한 결정론을 보장하지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>None. 전역 난수 상태를 바꾼다.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-3">복사</button></div><pre id="template-code-07-3" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 3"><code class="language-python">import hdat_templates as h
import numpy as np
h.seed_everything(42)
first = np.random.rand(3)
h.seed_everything(42)
second = np.random.rand(3)
assert np.array_equal(first, second)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>모델 생성·shuffle·무작위 분할 전에 호출한다.</li>
<li>여러 장치·라이브러리 버전·비결정적 연산까지 결과 동일성을 보장하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-quick_audit"><code>quick_audit</code></h4>
<p>표의 크기·중복·dtype·결측·고유값·정답 요약을 제한된 분량으로 돌려준다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>df</code></td>
<td>필수</td>
<td><code>train_df.copy()</code></td>
<td>현재 처리할 원본 표 또는 분할한 표. pandas DataFrame. 열 이름/순서·인덱스를 확인한다.</td>
</tr>
<tr>
<td><code>target_cols</code></td>
<td><code>()</code></td>
<td><code>('fuel_use',)</code></td>
<td>문제 설명의 정답 열 이름과 출력 순서. 문자열의 list/tuple. 한 열 tuple에도 쉼표가 필요하다. ('fuel_use')는 문자열이다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>dict. shape tuple, 개수, Series, target_summary dict 등이 섞여 있다.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-4">복사</button></div><pre id="template-code-07-4" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 4"><code class="language-python">import hdat_templates as h
import pandas as pd
df = pd.DataFrame({"speed": [10., None, 20.], "fault": [0, 1, 0]})
report = h.quick_audit(df, target_cols=("fault",))
print(report["shape"])
print(report["target_summary"]["fault"])
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>target_cols에 없는 열 이름은 오류 없이 요약 대상에서 빠지므로 오타를 별도로 확인한다.</li>
<li>중복 열을 찾아 보여 주지만 정답 열 이름이 중복되면 target_summary 처리도 실패할 수 있다.</li>
<li>nunique_top20은 고유값 수가 작은 순이다. 이름의 top이 큰 순이라는 뜻은 아니다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-assert_frame_contract"><code>assert_frame_contract</code></h4>
<p>훈련 정답과 테스트 특성의 열 계약을 검사한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>train</code></td>
<td>필수</td>
<td><code>train_df</code></td>
<td>제공된 훈련 파일을 읽은 표 또는 통계를 배울 훈련 폴드. DataFrame. wrapper에는 정답 열 포함, 통계 fit helper에는 훈련 부분만.</td>
</tr>
<tr>
<td><code>test</code></td>
<td>필수</td>
<td><code>test_df</code></td>
<td>정답 없이 제공된 테스트 파일의 표. DataFrame. 제출 대상 행의 원래 순서를 보존한다.</td>
</tr>
<tr>
<td><code>target_cols</code></td>
<td>필수</td>
<td><code>('fuel_use',)</code></td>
<td>문제 설명의 정답 열 이름과 출력 순서. 문자열의 list/tuple. 한 열 tuple에도 쉼표가 필요하다. ('fuel_use')는 문자열이다.</td>
</tr>
<tr>
<td><code>ignore_feature_cols</code></td>
<td><code>()</code></td>
<td><code>('record_id',)</code></td>
<td>ID·누수 열처럼 모델 입력에서 제외하기로 한 실제 열. 열 이름 목록. train에 없는 이름은 오류. 이 함수는 검사만 하고 실제 삭제하지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>None. assert/KeyError로 실패를 알리고 일부 추가 열은 경고만 출력.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-5">복사</button></div><pre id="template-code-07-5" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 5"><code class="language-python">import hdat_templates as h
import pandas as pd
train = pd.DataFrame({"id": [1, 2], "speed": [10., 20.], "target": [1., 2.]})
test = pd.DataFrame({"id": [3], "speed": [15.]})
h.assert_frame_contract(train, test, target_cols=("target",),
                       ignore_feature_cols=("id",))
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>정답은 train에 있어야 하고 test에는 없어야 한다. 필요한 특성이 빠졌으면 실패한다.</li>
<li>test의 추가 열은 즉시 실패가 아니라 경고다. 열 순서를 정렬하거나 dtype를 비교하지 않는다.</li>
<li>ignore_feature_cols는 검사에서 제외할 뿐 표에서 열을 지우지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-minmax_selected"><code>minmax_selected</code></h4>
<p>지정 열을 현재 표의 최솟값·최댓값으로 0..1 스케일링한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>df</code></td>
<td>필수</td>
<td><code>train_df.copy()</code></td>
<td>현재 처리할 원본 표 또는 분할한 표. pandas DataFrame. 열 이름/순서·인덱스를 확인한다.</td>
</tr>
<tr>
<td><code>columns</code></td>
<td>필수</td>
<td><code>['speed', 'temperature']</code></td>
<td>문제에서 처리하라고 지정한 실제 열 이름. 문자열 목록. df 전체나 열 값 자체가 아닌 열 이름을 넣는다. 대상 데이터는 해당 연산이 가능한 dtype이어야 한다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>원래 인덱스·비대상 열을 보존한 복사 DataFrame. 선택 열은 float로 변환.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-6">복사</button></div><pre id="template-code-07-6" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 6"><code class="language-python">import hdat_templates as h
import pandas as pd
df = pd.DataFrame({"speed": [10., 20., 30.], "id": ["a", "b", "c"]})
scaled = h.minmax_selected(df, columns=["speed"])
assert scaled["speed"].tolist() == [0.0, 0.5, 1.0]
assert df["speed"].tolist() == [10.0, 20.0, 30.0]
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>같은 호출의 표에서 통계를 매번 계산한다. train/valid/test에 각각 호출하면 서로 다른 척도를 쓰므로 예측 전처리의 train-fit/valid-transform 대체품이 아니다.</li>
<li>상수 열은 유효값만 0, NaN은 보존. 전부 NaN인 열은 NaN 유지.</li>
<li>숫자로 변환 가능한 문자열은 변환하지만 Inf·중복 열·비수치 입력을 완전히 방어하는 함수는 아니다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-fit_iqr_bounds"><code>fit_iqr_bounds</code></h4>
<p>훈련 표에서만 IQR clipping 하한·상한을 계산한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>train</code></td>
<td>필수</td>
<td><code>X.iloc[tr_idx]</code></td>
<td>통계를 배워도 되는 훈련 폴드만 선택한 표. DataFrame. 지정 columns는 수치형이고 train 통계만 이후 valid/test에 적용.</td>
</tr>
<tr>
<td><code>columns</code></td>
<td>필수</td>
<td><code>['speed', 'temperature']</code></td>
<td>문제에서 처리하라고 지정한 실제 열 이름. 문자열 목록. df 전체나 열 값 자체가 아닌 열 이름을 넣는다. 대상 데이터는 해당 연산이 가능한 dtype이어야 한다.</td>
</tr>
<tr>
<td><code>whisker</code></td>
<td><code>1.5</code></td>
<td><code>1.5</code></td>
<td>IQR 경계 식의 배수: 문제 지시 또는 검증에서 정한 설정. 실수 하나. 통상 0 이상으로 사용하며 helper 자체는 음수 등을 별도 검증하지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>dict[str,tuple[float,float]]. 해당 열을 아직 변환하지 않는다.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-7">복사</button></div><pre id="template-code-07-7" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 7"><code class="language-python">import hdat_templates as h
import pandas as pd
train_fold = pd.DataFrame({"speed": [0., 2., 4., 6.]})
bounds = h.fit_iqr_bounds(train_fold, columns=["speed"], whisker=1.5)
assert bounds["speed"] == (-3.0, 9.0)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>split 후 train 폴드에만 fit하고 동일 bounds를 valid/test에 재사용한다.</li>
<li>IQR=Q3−Q1, 하한 Q1−whisker×IQR, 상한 Q3+whisker×IQR.</li>
<li>전체 NaN·상수·Inf 등의 경계를 별도로 확인한다. 자동 삭제 함수가 아니다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-apply_clip_bounds"><code>apply_clip_bounds</code></h4>
<p>이미 배운 열별 하한·상한으로 값을 경계에 눌러 붙인다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>df</code></td>
<td>필수</td>
<td><code>train_df.copy()</code></td>
<td>현재 처리할 원본 표 또는 분할한 표. pandas DataFrame. 열 이름/순서·인덱스를 확인한다.</td>
</tr>
<tr>
<td><code>bounds</code></td>
<td>필수</td>
<td><code>bounds</code></td>
<td>h.fit_iqr_bounds(train_fold, columns)의 반환값. {열 이름: (하한, 상한)} 딕셔너리. valid/test에서 새로 계산하지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>복사 DataFrame. 대상 열의 초과값만 clip하며 행을 삭제하지 않는다.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-8">복사</button></div><pre id="template-code-07-8" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 8"><code class="language-python">import hdat_templates as h
import pandas as pd
train_fold = pd.DataFrame({"speed": [0., 2., 4., 6.]})
valid_fold = pd.DataFrame({"speed": [-10., 20.]})
bounds = h.fit_iqr_bounds(train_fold, ["speed"])
clipped = h.apply_clip_bounds(valid_fold, bounds)
assert clipped["speed"].tolist() == [-3.0, 9.0]
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>bounds를 valid/test에서 다시 학습하지 않는다.</li>
<li>범위를 벗어난 행을 제거하거나 이상치 라벨을 만드는 함수가 아니다.</li>
<li>존재하지 않는 bounds 열은 오류. 양 끝·NaN 경계를 호출 전 점검한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-add_datetime_features"><code>add_datetime_features</code></h4>
<p>지정 날짜 열에서 연·월·일·요일·시·분을 만든다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>df</code></td>
<td>필수</td>
<td><code>train_df.copy()</code></td>
<td>현재 처리할 원본 표 또는 분할한 표. pandas DataFrame. 열 이름/순서·인덱스를 확인한다.</td>
</tr>
<tr>
<td><code>columns</code></td>
<td>필수</td>
<td><code>['time']</code></td>
<td>날짜 파생변수를 만들 원본 날짜 열의 이름 목록. 예제의 df에는 time 열이 있으므로 ['time']을 넣는다. 날짜 값 자체나 수치 센서 열 이름을 넣는 자리가 아니다.</td>
</tr>
<tr>
<td><code>drop_original</code></td>
<td><code>True</code></td>
<td><code>True</code></td>
<td>날짜 원래 열을 모델 입력에서 없앨지 결정. bool. True면 날짜 파생 열을 만들고 원래 열을 삭제한다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>복사 DataFrame. 각 열에 __year/__month/__day/__dow/__hour/__minute를 추가.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-9">복사</button></div><pre id="template-code-07-9" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 9"><code class="language-python">import hdat_templates as h
import pandas as pd
df = pd.DataFrame({"time": ["2026-09-08 13:45", "bad-date"]})
result = h.add_datetime_features(df, columns=["time"], drop_original=True)
print(result[["time__year", "time__dow", "time__hour"]])
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>문자열 날짜를 errors='coerce'로 해석하므로 잘못된 날짜는 NaT/파생 NaN.</li>
<li>dayofweek는 월요일 0부터 일요일 6. 원래 열은 drop_original=True일 때 삭제한다.</li>
<li>이 함수는 시간순으로 정렬하지 않는다. 같은 이름의 기존 파생 열이 있으면 덮어쓸 수 있다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-clean_tabular_values"><code>clean_tabular_values</code></h4>
<p>수치 Inf를 NaN으로 바꾸고 범주형 혼합값을 문자열 표현으로 통일한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>df</code></td>
<td>필수</td>
<td><code>train_df.copy()</code></td>
<td>현재 처리할 원본 표 또는 분할한 표. pandas DataFrame. 열 이름/순서·인덱스를 확인한다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>복사 DataFrame. 숫자 열은 Inf→NaN, 다른 열은 문자열 또는 np.nan.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-10">복사</button></div><pre id="template-code-07-10" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 10"><code class="language-python">import hdat_templates as h
import numpy as np
import pandas as pd
df = pd.DataFrame({"value": [1., np.inf], "category": ["A", None]})
cleaned = h.clean_tabular_values(df)
assert pd.isna(cleaned.loc[1, "value"])
assert pd.isna(cleaned.loc[1, "category"])
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>결측 대체·범주 인코딩·표준화를 수행하지 않는다. 후속 전처리기의 입력을 정리한다.</li>
<li>datetime·문자열 수치는 비수치 범주로 취급될 수 있어 날짜 분해/수치 변환 여부를 먼저 결정한다.</li>
<li>target에 무작정 적용하지 말고 모델 입력 X를 정리하는 데 사용한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-crop_to_numpy"><code>crop_to_numpy</code></h4>
<p>단일 PIL 이미지의 지정 좌표를 잘라 NumPy 배열로 돌려준다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>image</code></td>
<td>필수</td>
<td><code>image</code></td>
<td>PIL로 이미 읽은 단일 이미지. PIL Image처럼 crop 메서드를 가진 객체. 파일 경로 문자열이나 배치 배열이 아니다.</td>
</tr>
<tr>
<td><code>box</code></td>
<td>필수</td>
<td><code>(2, 1, 7, 5)</code></td>
<td>문제의 자르기 좌표 또는 이미지 폭·높이로 계산한 경계. (left, upper, right, lower)의 정수 4개. x=2..6, y=1..4 영역으로 폭 5·높이 4. 중앙 crop 크기 2개를 넣는 인자가 아니다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>잘린 영역의 copy ndarray. RGB (H,W,3), 흑백 (H,W) 등 원래 모드의 dtype/채널 유지.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-11">복사</button></div><pre id="template-code-07-11" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 11"><code class="language-python">import hdat_templates as h
from PIL import Image
image = Image.new("RGB", (10, 7), color=(255, 0, 0))  # PIL은 (W,H)
patch = h.crop_to_numpy(image, box=(2, 1, 7, 5))
assert patch.shape == (4, 5, 3)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>PIL Image가 필요하다. 경로 문자열이나 (x,y,width,height) 표현은 인자가 아니다.</li>
<li>오른쪽·아래 끝은 제외된다. 좌표 유효성·원본 범위·중앙 맞춤은 이 wrapper가 별도로 검사하지 않는다.</li>
<li>반환값을 PyTorch에 넣으려면 배치/채널 축과 값 척도 준비가 더 필요하다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-conv_output_size"><code>conv_output_size</code></h4>
<p>한 합성곱 공간 축의 출력 길이를 계산한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>input_size</code></td>
<td>필수</td>
<td><code>32</code></td>
<td>현재 합성곱에 들어갈 한 공간 축의 길이. 양의 정수. 높이와 너비는 각각 호출. 배치 크기·채널 수가 아니다.</td>
</tr>
<tr>
<td><code>kernel_size</code></td>
<td>필수</td>
<td><code>3</code></td>
<td>문제에 지정된 합성곱 커널의 해당 축 길이. 양의 정수. 이 helper는 2D tuple이 아닌 한 축의 정수 계산.</td>
</tr>
<tr>
<td><code>stride</code></td>
<td><code>1</code></td>
<td><code>2</code></td>
<td>문제 명세의 합성곱 이동 간격. 양의 정수. 커널 시작 위치가 한 공간 축에서 움직이는 간격이다.</td>
</tr>
<tr>
<td><code>padding</code></td>
<td><code>0</code></td>
<td><code>1</code></td>
<td>합성곱 한쪽에 붙이는 padding 길이. 0 이상 정수. 양쪽 합이 아니라 한쪽 값이며 식에서 2P로 계산.</td>
</tr>
<tr>
<td><code>dilation</code></td>
<td><code>1</code></td>
<td><code>1</code></td>
<td>합성곱 커널 원소 사이의 간격 설정. 양의 정수. 유효 커널 크기는 D(K−1)+1.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>Python int. floor((I+2P−D(K−1)−1)/S+1).</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-12">복사</button></div><pre id="template-code-07-12" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 12"><code class="language-python">import hdat_templates as h
out_height = h.conv_output_size(input_size=32, kernel_size=3,
                               stride=2, padding=1, dilation=1)
out_width = h.conv_output_size(input_size=40, kernel_size=3,
                              stride=2, padding=1, dilation=1)
assert (out_height, out_width) == (16, 20)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>한 축 계산기이므로 높이·너비가 다르면 두 번 호출한다.</li>
<li>유효한 양의 input/kernel/stride/dilation과 0 이상 padding을 호출자가 보장해야 한다.</li>
<li>채널 수·학습 파라미터 수를 반환하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-safe_train_valid_indices"><code>safe_train_valid_indices</code></h4>
<p>행 무작위·시간·그룹 중 지정한 한 방법으로 train/valid 행 위치를 만든다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>X</code></td>
<td>필수</td>
<td><code>X</code></td>
<td>아직 train/valid를 나누지 않은 입력 표. DataFrame N행, N≥2. y/time/groups의 원래 행 순서와 일치.</td>
</tr>
<tr>
<td><code>y</code></td>
<td>필수</td>
<td><code>y_train</code></td>
<td>X의 각 행과 정확히 대응하는 정답. 회귀 (N,) 또는 (N,D); 이진 (N,) 또는 (N,1) 0/1; 다중분류 (N,) class index; 다중라벨 (N,K) 0/1. sklearn 함수는 원래 클래스 라벨도 가능.</td>
</tr>
<tr>
<td><code>task</code></td>
<td>필수</td>
<td><code>'binary'</code></td>
<td>정답이 연속값인지, 이진/다중분류/다중라벨인지 문제에서 판별. 'regression'|'binary'|'multiclass'|'multilabel'의 정확한 문자열. 출력·정답·loss 계약이 함께 바뀐다. 모든 함수가 오타를 즉시 검사하는 것은 아니다.</td>
</tr>
<tr>
<td><code>valid_size</code></td>
<td><code>0.2</code></td>
<td><code>0.2</code></td>
<td>검증에 남길 비율: 문제 조건 또는 사전에 고정한 계획. 0&lt;값&lt;1. 무작위/시간은 행 기준, 그룹 분할에서는 고유 그룹 기준 비율.</td>
</tr>
<tr>
<td><code>seed</code></td>
<td><code>42</code></td>
<td><code>42</code></td>
<td>본인이 고정한 실험 재현용 정수. 문제에 지정값이 있으면 그것을 사용. 정수 하나. 같은 seed가 모든 GPU 연산의 완전한 결정론을 보장하지 않는다.</td>
</tr>
<tr>
<td><code>time_col</code></td>
<td><code>None</code></td>
<td><code>'recorded_at'</code></td>
<td>시간순 검증에 쓸 실제 열 이름. 문자열 또는 None. time_values가 제공되면 그것이 우선. group split과 동시에 쓰지 않는다.</td>
</tr>
<tr>
<td><code>time_values</code></td>
<td><code>None</code></td>
<td><code>train_df['recorded_at'].to_numpy()</code></td>
<td>X와 같은 행 순서로 보관한 실제 시간 열 값. 길이 N의 1D. time_col보다 우선하며 pd.to_datetime으로 변환할 수 있어야 한다.</td>
</tr>
<tr>
<td><code>groups</code></td>
<td><code>None</code></td>
<td><code>train_df['vehicle_id'].to_numpy()</code></td>
<td>X/features 각 행에 대응하는 차량·주행·개체 ID 값. 길이 N 또는 T의 1D, 결측 없음. 열 이름 문자열 자체를 넣지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>(tr_idx,va_idx): 1D 정수 NumPy 위치 배열.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-13">복사</button></div><pre id="template-code-07-13" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 13"><code class="language-python">import hdat_templates as h
import numpy as np
import pandas as pd
X = pd.DataFrame({"speed": np.arange(6, dtype=float)})
y = np.arange(6, dtype=float)
groups = np.array(["A", "A", "B", "B", "C", "C"])
tr_idx, va_idx = h.safe_train_valid_indices(
    X, y, task="regression", valid_size=1/3, groups=groups)
assert not set(groups[tr_idx]) &amp; set(groups[va_idx])
X_train, X_valid = X.iloc[tr_idx], X.iloc[va_idx]
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>X/y는 같은 N행, N≥2. 반환값은 .iloc 또는 NumPy 위치 인덱싱에 쓴다. .loc와 혼동하지 않는다.</li>
<li>time_values/time_col과 groups를 동시에 제공하면 오류. 두 목표를 결합한 검증은 별도 구현.</li>
<li>time은 stable한 시간 정렬 뒤 앞/뒤를 나누지만 horizon·정답 공개 시점은 모른다. 시계열 윈도 전용 함수를 검토한다.</li>
<li>group valid_size는 그룹 비율이며 행 비율이 정확히 같지 않을 수 있다.</li>
<li>무작위 분류의 stratify는 각 클래스 개수와 양쪽 분할 크기가 허용할 때만 켜진다. 클래스 보존은 별도 검사한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-make_preprocessor"><code>make_preprocessor</code></h4>
<p>입력 표의 수치/범주 열에 맞는 미학습 전처리기를 만든다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>X</code></td>
<td>필수</td>
<td><code>X.iloc[tr_idx]</code></td>
<td>분할한 훈련 폴드의 입력 표. pandas DataFrame (N_train,F_raw). 함수 생성 시 열/dtype만 읽으며 fit은 이후 train에만.</td>
</tr>
<tr>
<td><code>encoding</code></td>
<td><code>'onehot'</code></td>
<td><code>'ordinal'</code></td>
<td>범주 열의 고유값 수·메모리와 모델에 맞춰 정한 표현. 'onehot'|'ordinal'만 사용. 현 구현은 onehot 이외 문자열을 ordinal로 처리하므로 오타 주의.</td>
</tr>
<tr>
<td><code>scale_numeric</code></td>
<td><code>False</code></td>
<td><code>True</code></td>
<td>거리/선형 모델 등에 수치 표준화를 적용할지 결정. bool. True면 중앙값 대체 후 StandardScaler; missing indicator도 수치 파이프라인 안에서 변환된다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>미학습 ColumnTransformer. fit_transform 후 (N,F_out)의 수치 배열 또는 sparse matrix.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-14">복사</button></div><pre id="template-code-07-14" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 14"><code class="language-python">import hdat_templates as h
import pandas as pd
X_train = pd.DataFrame({"speed": [10., None, 30.], "road": ["city", "road", "city"]})
X_valid = pd.DataFrame({"speed": [20.], "road": ["new"]})
prep = h.make_preprocessor(X_train, encoding="ordinal", scale_numeric=True)
X_train_s = prep.fit_transform(X_train)
X_valid_s = prep.transform(X_valid)
assert X_train_s.shape[1] == X_valid_s.shape[1]
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>생성할 때 X는 열·dtype를 고르는 용도다. 통계를 배우는 실제 fit은 train 폴드에만 해야 한다.</li>
<li>수치: 중앙값 대체+결측 indicator, 선택적 표준화. 범주: 결측 문자열 대체+onehot/ordinal.</li>
<li>OneHotEncoder를 썼다고 최종 출력이 항상 sparse나 float32라는 보장은 없다. 결합 결과의 실제 타입·dtype·폭을 확인한다.</li>
<li>unseen 범주는 onehot 0벡터 또는 ordinal −1로 처리. 열 이름/순서는 후속 입력에도 동일하게 유지한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-make_tabular_estimator"><code>make_tabular_estimator</code></h4>
<p>과제·모델 이름에 맞는 미학습 sklearn estimator를 만든다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>task</code></td>
<td>필수</td>
<td><code>'binary'</code></td>
<td>정답이 연속값인지, 이진/다중분류/다중라벨인지 문제에서 판별. 'regression'|'binary'|'multiclass'|'multilabel'의 정확한 문자열. 출력·정답·loss 계약이 함께 바뀐다. 모든 함수가 오타를 즉시 검사하는 것은 아니다.</td>
</tr>
<tr>
<td><code>model_name</code></td>
<td><code>'extra_trees'</code></td>
<td><code>'linear'</code></td>
<td>문제 명세 또는 검증 예산으로 고른 sklearn 기준 모델 종류. 'linear'|'extra_trees'. 'MLP'나 이미 생성된 객체가 아니다.</td>
</tr>
<tr>
<td><code>seed</code></td>
<td><code>42</code></td>
<td><code>42</code></td>
<td>본인이 고정한 실험 재현용 정수. 문제에 지정값이 있으면 그것을 사용. 정수 하나. 같은 seed가 모든 GPU 연산의 완전한 결정론을 보장하지 않는다.</td>
</tr>
<tr>
<td><code>multioutput</code></td>
<td><code>False</code></td>
<td><code>True</code></td>
<td>서로 다른 정답 열이 여러 개인지 shape로 판단. bool. 다중분류의 클래스 수 C가 3이라는 이유만으로 True가 되는 것은 아니다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>Ridge/LogisticRegression/ExtraTreesRegressor/ExtraTreesClassifier 또는 MultiOutputClassifier.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-15">복사</button></div><pre id="template-code-07-15" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 15"><code class="language-python">import hdat_templates as h
import numpy as np
X = np.array([[0.], [1.], [2.], [3.]])
y = np.array([0., 2., 4., 6.])
estimator = h.make_tabular_estimator(task="regression", model_name="linear")
estimator.fit(X, y)  # 이 작은 예시는 API 동작 확인용
pred = estimator.predict(np.array([[1.5]]))
assert pred.shape == (1,)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>task='regression'인지에 따라 회귀/분류가 나뉜다. 허용 문자열을 정확히 쓰고 typo가 분류로 흘러가지 않게 한다.</li>
<li>multioutput은 정답 열 여러 개를 뜻한다. 클래스 3종의 단일 정답은 multiclass,multioutput=False다.</li>
<li>입력 전처리를 수행하지 않으며 fit도 하지 않는다. 비수치·결측 입력은 먼저 처리한다.</li>
<li>모델 내부 하이퍼파라미터는 이 생성기의 고정값이며 문제의 정확한 모델 명세와 다른지 대조한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-evaluate_predictions"><code>evaluate_predictions</code></h4>
<p>실제 정답·예측·필요한 확률로 지원 지표 하나를 계산한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>y_true</code></td>
<td>필수</td>
<td><code>y_valid</code></td>
<td>검증 폴드의 실제 정답, 공식 단위·라벨로 준비. 예측과 동일한 행 순서. 분류 지표의 라벨 정의와 다중출력 집계 정책도 맞춰야 한다.</td>
</tr>
<tr>
<td><code>y_pred</code></td>
<td>필수</td>
<td><code>valid_pred</code></td>
<td>현재 모델의 검증 예측값 또는 확정 라벨. MSE 등은 연속값, accuracy/F1은 확정 라벨. AUC에서 y_score를 주면 y_pred 대신 그 점수를 사용.</td>
</tr>
<tr>
<td><code>metric</code></td>
<td>필수</td>
<td><code>'f1_macro'</code></td>
<td>문제에서 정한 공식 평가 지표 이름. mse|rmse|mae|rmsle|accuracy|f1|f1_macro|auc|roc_auc. f1도 이 helper에서는 macro이며 양성 F1 별칭이 아니다.</td>
</tr>
<tr>
<td><code>y_score</code></td>
<td><code>None</code></td>
<td><code>valid_probability</code></td>
<td>ROC-AUC에 사용할 연속 점수 또는 확률. 이진 (N,) 또는 (N,1)/(N,2), 다중분류 (N,C) 확률, 다중라벨 (N,K). None이면 y_pred 사용. 라벨 점수 대신 연속값 권장.</td>
</tr>
<tr>
<td><code>task</code></td>
<td><code>None</code></td>
<td><code>'multilabel'</code></td>
<td>특히 다중라벨 평가 집계를 명시할 때 사용하는 과제 구분. None 가능. multilabel이면 subset accuracy/label-macro F1. 명시하지 않으면 다른 다중출력 분류 집계가 적용될 수 있다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>Python float. 학습/모델 선택/제출을 하지 않는다.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-16">복사</button></div><pre id="template-code-07-16" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 16"><code class="language-python">import hdat_templates as h
import numpy as np
y_true = np.array([0, 1, 1, 0])
prob = np.array([0.1, 0.8, 0.6, 0.7])
label = (prob &gt;= 0.5).astype(int)
macro_f1 = h.evaluate_predictions(y_true, label, metric="f1_macro", task="binary")
auc = h.evaluate_predictions(y_true, label, metric="roc_auc",
                            y_score=prob, task="binary")
print(macro_f1, auc)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>metric='f1'와 'f1_macro' 모두 macro F1. 양성 클래스 하나의 binary F1과 다르다.</li>
<li>task='multilabel'의 accuracy는 전체 라벨이 모두 맞아야 맞는 subset accuracy; F1은 라벨별 양성 F1 평균.</li>
<li>다른 다중출력 분류는 열별 accuracy/macro-F1 평균. 공식 산식이 다르면 별도 구현한다.</li>
<li>RMSLE는 음수 y_true를 거절하지만 y_pred는 0으로 clip한다. 음수 예측을 오류로 처리하는 명세와 다르다.</li>
<li>AUC는 연속 점수가 필요하다. 2열은 두 번째 열을, 1열은 첫 열을 사용한다. positive_label/class_order 인자가 없어 원래 라벨과 열 대응을 호출자가 맞춰야 한다.</li>
<li>다중분류 AUC 확률 열은 sklearn의 클래스 순서와 일치해야 하며 각 행 확률 합 등 조건도 맞아야 한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-assert_class_fold_coverage"><code>assert_class_fold_coverage</code></h4>
<p>분류 train/valid에 필요한 클래스가 들어 있는지 검사한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>y</code></td>
<td>필수</td>
<td><code>y_train</code></td>
<td>X의 각 행과 정확히 대응하는 정답. 회귀 (N,) 또는 (N,D); 이진 (N,) 또는 (N,1) 0/1; 다중분류 (N,) class index; 다중라벨 (N,K) 0/1. sklearn 함수는 원래 클래스 라벨도 가능.</td>
</tr>
<tr>
<td><code>train_indices</code></td>
<td>필수</td>
<td><code>tr_idx</code></td>
<td>분할 함수가 반환한 훈련 행 위치. 1D 정수 배열. DataFrame 인덱스 라벨이 아니라 0..N−1 위치.</td>
</tr>
<tr>
<td><code>valid_indices</code></td>
<td>필수</td>
<td><code>va_idx</code></td>
<td>분할 함수가 반환한 검증 행 위치. 1D 정수 배열. y와 같은 원래 행 배열을 기준으로 한다.</td>
</tr>
<tr>
<td><code>task</code></td>
<td>필수</td>
<td><code>'binary'</code></td>
<td>정답이 연속값인지, 이진/다중분류/다중라벨인지 문제에서 판별. 'regression'|'binary'|'multiclass'|'multilabel'의 정확한 문자열. 출력·정답·loss 계약이 함께 바뀐다. 모든 함수가 오타를 즉시 검사하는 것은 아니다.</td>
</tr>
<tr>
<td><code>metric</code></td>
<td>필수</td>
<td><code>'f1_macro'</code></td>
<td>문제에서 정한 공식 평가 지표 이름. mse|rmse|mae|rmsle|accuracy|f1|f1_macro|auc|roc_auc. f1도 이 helper에서는 macro이며 양성 F1 별칭이 아니다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>None 또는 ValueError.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-17">복사</button></div><pre id="template-code-07-17" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 17"><code class="language-python">import hdat_templates as h
import numpy as np
y = np.array([0, 1, 0, 1])
tr_idx, va_idx = np.array([0, 1]), np.array([2, 3])
h.assert_class_fold_coverage(y, tr_idx, va_idx,
                           task="binary", metric="roc_auc")
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>분할 후 모델 fit 전에 호출. task가 회귀면 검사 없이 반환한다.</li>
<li>검증에만 존재하는 클래스를 거절하고 binary/multiclass train 클래스 1개도 거절한다.</li>
<li>AUC는 양쪽에 2개 이상 클래스 필요; multiclass AUC는 양쪽 클래스 집합도 같아야 한다.</li>
<li>라벨 자체의 모든 dtype/0..C−1 계약이나 같은 그룹 중복까지 검사하는 함수는 아니다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-fit_tabular_baseline"><code>fit_tabular_baseline</code></h4>
<p>표 데이터의 분할·train-only 전처리·검증·전체 재학습·테스트 예측을 연결한 연습용 wrapper다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>train</code></td>
<td>필수</td>
<td><code>train_df</code></td>
<td>제공된 훈련 파일을 읽은 표 또는 통계를 배울 훈련 폴드. DataFrame. wrapper에는 정답 열 포함, 통계 fit helper에는 훈련 부분만.</td>
</tr>
<tr>
<td><code>test</code></td>
<td>필수</td>
<td><code>test_df</code></td>
<td>정답 없이 제공된 테스트 파일의 표. DataFrame. 제출 대상 행의 원래 순서를 보존한다.</td>
</tr>
<tr>
<td><code>cfg</code></td>
<td>필수</td>
<td><code>h.TabularConfig(task='regression', target_cols=('fuel_use',), metric='rmse')</code></td>
<td>h.TabularConfig(...)를 호출해 만든 설정 객체. TabularConfig 인스턴스. 딕셔너리·클래스 자체·Torch config 객체가 아니다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>(pipe,test_pred,metric_value). pipe는 마지막 전체 train 재학습 모델; metric_value는 앞선 holdout 모델의 검증 점수.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-18">복사</button></div><pre id="template-code-07-18" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 18"><code class="language-python">import hdat_templates as h
import pandas as pd
train = pd.DataFrame({"speed": [0.,1.,2.,3.,4.,5.,6.,7.],
                      "fuel": [0.,2.,4.,6.,8.,10.,12.,14.]})
test = pd.DataFrame({"speed": [2.5, 4.5]})
cfg = h.TabularConfig(task="regression", target_cols=("fuel",),
                     metric="rmse", model_name="linear",
                     output_kind="value", valid_size=0.25)
pipe, test_pred, valid_rmse = h.fit_tabular_baseline(train, test, cfg)
assert test_pred.shape == (2,)
# 아직 파일 저장이나 실제 제출은 이루어지지 않았다.
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>실제 시험의 제공 skeleton/함수명/저장 셀을 대신하는 무수정 end-to-end 호출로 안내하면 안 된다.</li>
<li>cfg는 TabularConfig 객체. train은 정답 포함, test는 정답 미포함. binary/multiclass는 정답 열 하나만 지원.</li>
<li>group/time 값은 특성 삭제 전에 확보한다. group_col은 자동 삭제되지 않음.</li>
<li>valid 결과를 출력한 뒤 자동으로 전체 train 재학습. 반환 pipe가 holdout-only 모델이라고 생각하면 안 된다.</li>
<li>AUC여도 제출 label/probability는 별도 명세. output_kind=None이면 오류가 늦게 발생하므로 시작 전에 명시한다.</li>
<li>저장/실제 제출/임계값 최적화는 하지 않는다. positive_label/class_order는 확률 제출에 반영된다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-predict_tabular_probabilities"><code>predict_tabular_probabilities</code></h4>
<p>학습된 sklearn Pipeline에서 제출 라벨 의미·클래스 순서에 맞춘 확률을 고른다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>pipe</code></td>
<td>필수</td>
<td><code>pipe</code></td>
<td>전처리와 model 이름의 estimator가 들어 있고 fit을 마친 Pipeline. sklearn Pipeline. predict_proba 및 named_steps['model'].classes_가 있어야 한다.</td>
</tr>
<tr>
<td><code>X</code></td>
<td>필수</td>
<td><code>X_test</code></td>
<td>학습된 Pipeline에 같은 열 순서로 전달할 검증/테스트 입력 표. DataFrame (N,F_raw). 이미 전처리한 배열을 Pipeline에 중복 전달하지 않는다.</td>
</tr>
<tr>
<td><code>task</code></td>
<td>필수</td>
<td><code>'binary'</code></td>
<td>정답이 연속값인지, 이진/다중분류/다중라벨인지 문제에서 판별. 'regression'|'binary'|'multiclass'|'multilabel'의 정확한 문자열. 출력·정답·loss 계약이 함께 바뀐다. 모든 함수가 오타를 즉시 검사하는 것은 아니다.</td>
</tr>
<tr>
<td><code>positive_label</code></td>
<td><code>1</code></td>
<td><code>'fault'</code></td>
<td>이진 확률 열이 의미해야 할 원래 정답 라벨. 학습된 classes_ 안의 값. 제출 확률 열 선택에 쓰며 훈련 라벨을 자동 0/1 매핑하지 않는다.</td>
</tr>
<tr>
<td><code>class_order</code></td>
<td><code>None</code></td>
<td><code>('normal', 'minor', 'major')</code></td>
<td>제출 확률 열의 정해진 클래스 순서. 모든 학습 클래스를 중복 없이 포함한 순서. None은 estimator classes_ 순서.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>binary (N,), multiclass (N,C), multilabel (N,K)의 NumPy 확률 배열.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-19">복사</button></div><pre id="template-code-07-19" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 19"><code class="language-python">import hdat_templates as h
import pandas as pd
from sklearn.pipeline import Pipeline
X = pd.DataFrame({"speed": [0., 1., 2., 3.]})
y = ["normal", "normal", "fault", "fault"]
pipe = Pipeline([("prep", h.make_preprocessor(X)),
                 ("model", h.make_tabular_estimator("binary", "linear"))])
pipe.fit(X, y)
prob_fault = h.predict_tabular_probabilities(pipe, X, task="binary",
                                            positive_label="fault")
assert prob_fault.shape == (4,)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>pipe에는 named_steps['model'].classes_가 있어야 한다. raw estimator만 넣으면 안 된다.</li>
<li>binary positive_label은 실제 classes_에 있어야 한다. None/기본 1이 문자열 'fault'를 대신하지 않는다.</li>
<li>multiclass class_order는 모든 학습 클래스를 중복 없이 포함해야 한다.</li>
<li>multilabel은 0/1 indicator이고 학습에서 양성 1이 전혀 없는 열의 확률은 0으로 만든다. 회귀는 지원하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-make_point_forecast_windows"><code>make_point_forecast_windows</code></h4>
<p>정렬된 한 시계열에서 입력 윈도와 마지막 관측 이후의 정답을 함께 꺼낸다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>features</code></td>
<td>필수</td>
<td><code>series_df[['speed', 'temperature']].to_numpy(dtype=np.float32)</code></td>
<td>정답·ID를 제외한 시간순 원시 특성 행. (T,F), 1D (T,)도 한 특성으로 확장 가능. 시간 축 T는 전체 원시 행 수. 그룹 함수에서는 원래 행 대응 유지.</td>
</tr>
<tr>
<td><code>targets</code></td>
<td>필수</td>
<td><code>series_df[['fuel_use']].to_numpy(dtype=np.float32)</code></td>
<td>각 원시 행 시점의 정답 값 배열. (T,) 또는 (T,D), features와 같은 원시 행 길이. 미래 정답을 helper가 인덱스로 고르므로 이미 앞당긴 y를 다시 horizon만큼 이동하지 않는다.</td>
</tr>
<tr>
<td><code>lookback</code></td>
<td>필수</td>
<td><code>20</code></td>
<td>문제에서 사용할 과거 시간 길이를 샘플 개수로 환산. 양의 정수. 100Hz에서 0.2초를 20개로 해석하는 문제 관례. 초 0.2를 그대로 넣지 않는다.</td>
</tr>
<tr>
<td><code>horizon</code></td>
<td><code>1</code></td>
<td><code>40</code></td>
<td>마지막 입력 관측에서 목표까지의 간격을 행 개수로 환산. 0 이상 정수. 100Hz에서 0.4초 뒤는 40칸. 입력 시작이 아니라 마지막 입력에서 센다.</td>
</tr>
<tr>
<td><code>stride</code></td>
<td><code>1</code></td>
<td><code>1</code></td>
<td>윈도 시작 위치를 몇 행씩 이동할지 정한 값. 양의 정수. 윈도 helper에서는 초 단위가 아닌 시간순 행 간격.</td>
</tr>
<tr>
<td><code>assume_sorted</code></td>
<td><code>False</code></td>
<td><code>True</code></td>
<td>실제로 안정 시간순 정렬·행 대응을 확인한 뒤 명시하는 확인값. bool. True가 정렬을 실행해 주는 것은 아니다. 기본 False인 일반 윈도/분할/Lazy 호출은 오류를 낸다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>(Xw,yw,target_idx). shape (N,L,F),(N,D),(N,). X/y dtype는 원래 배열을 유지하고 인덱스는 정수.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-20">복사</button></div><pre id="template-code-07-20" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 20"><code class="language-python">import hdat_templates as h
import numpy as np
X = np.arange(24, dtype=np.float32).reshape(12, 2)
y = np.arange(12, dtype=np.float32)
Xw, yw, target_idx = h.make_point_forecast_windows(
    X, y, lookback=3, horizon=2, stride=1, assume_sorted=True)
assert Xw.shape == (8, 3, 2) and yw.shape == (8, 1)
assert target_idx.tolist() == list(range(4, 12))
assert yw[0, 0] == 4.0
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>입력 start=s이면 x=features[s:s+L], 정답 위치=s+L−1+H다. target_idx는 정렬된 원시 배열의 위치이다.</li>
<li>N=max(0,floor((T−L−H)/stride)+1). 길이가 부족하면 올바른 shape의 빈 배열 반환.</li>
<li>assume_sorted=True는 확인표시이며 정렬 기능은 없다. 다른 그룹을 이어 붙이면 경계를 넘으므로 grouped helper 사용.</li>
<li>정답 NaN·Inf를 자동 제거하지 않는다. 사전 유효성 검사와 이후 mask 선택이 필요하다.</li>
<li>전체 윈도를 np.stack으로 복제하므로 N×L×F 메모리를 먼저 계산한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-make_grouped_point_forecast_windows"><code>make_grouped_point_forecast_windows</code></h4>
<p>각 차량/주행 그룹 안에서만 정렬하고 윈도를 만들어 경계 횡단을 막는다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>features</code></td>
<td>필수</td>
<td><code>series_df[['speed', 'temperature']].to_numpy(dtype=np.float32)</code></td>
<td>정답·ID를 제외한 시간순 원시 특성 행. (T,F), 1D (T,)도 한 특성으로 확장 가능. 시간 축 T는 전체 원시 행 수. 그룹 함수에서는 원래 행 대응 유지.</td>
</tr>
<tr>
<td><code>targets</code></td>
<td>필수</td>
<td><code>series_df[['fuel_use']].to_numpy(dtype=np.float32)</code></td>
<td>각 원시 행 시점의 정답 값 배열. (T,) 또는 (T,D), features와 같은 원시 행 길이. 미래 정답을 helper가 인덱스로 고르므로 이미 앞당긴 y를 다시 horizon만큼 이동하지 않는다.</td>
</tr>
<tr>
<td><code>groups</code></td>
<td>필수</td>
<td><code>train_df['vehicle_id'].to_numpy()</code></td>
<td>X/features 각 행에 대응하는 차량·주행·개체 ID 값. 길이 N 또는 T의 1D, 결측 없음. 열 이름 문자열 자체를 넣지 않는다.</td>
</tr>
<tr>
<td><code>lookback</code></td>
<td>필수</td>
<td><code>20</code></td>
<td>문제에서 사용할 과거 시간 길이를 샘플 개수로 환산. 양의 정수. 100Hz에서 0.2초를 20개로 해석하는 문제 관례. 초 0.2를 그대로 넣지 않는다.</td>
</tr>
<tr>
<td><code>horizon</code></td>
<td><code>1</code></td>
<td><code>40</code></td>
<td>마지막 입력 관측에서 목표까지의 간격을 행 개수로 환산. 0 이상 정수. 100Hz에서 0.4초 뒤는 40칸. 입력 시작이 아니라 마지막 입력에서 센다.</td>
</tr>
<tr>
<td><code>stride</code></td>
<td><code>1</code></td>
<td><code>1</code></td>
<td>윈도 시작 위치를 몇 행씩 이동할지 정한 값. 양의 정수. 윈도 helper에서는 초 단위가 아닌 시간순 행 간격.</td>
</tr>
<tr>
<td><code>times</code></td>
<td><code>None</code></td>
<td><code>series_df['time'].to_numpy()</code></td>
<td>각 원시 행에 대응하는 실제 시간 값. 길이 T의 1D, 결측/파싱 실패 없음. 주면 각 그룹 안에서 stable sort한다.</td>
</tr>
<tr>
<td><code>assume_sorted</code></td>
<td><code>False</code></td>
<td><code>True</code></td>
<td>실제로 안정 시간순 정렬·행 대응을 확인한 뒤 명시하는 확인값. bool. True가 정렬을 실행해 주는 것은 아니다. 기본 False인 일반 윈도/분할/Lazy 호출은 오류를 낸다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>(Xw,yw,target_original_indices,window_groups). shape (N,L,F),(N,D),(N,),(N,).</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-21">복사</button></div><pre id="template-code-07-21" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 21"><code class="language-python">import hdat_templates as h
import numpy as np
import pandas as pd
X = np.arange(24, dtype=np.float32).reshape(12, 2)
y = np.arange(12, dtype=np.float32)
groups = np.array(["A"] * 6 + ["B"] * 6)
times = pd.date_range("2026-01-01", periods=12, freq="s")
Xw, yw, original_target, window_groups = h.make_grouped_point_forecast_windows(
    X, y, groups, lookback=3, horizon=1, times=times)
assert Xw.shape == (6, 3, 2)
assert original_target.tolist() == [3, 4, 5, 9, 10, 11]
assert window_groups.tolist() == ["A", "A", "A", "B", "B", "B"]
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>times를 주면 그룹 내부 stable sort; times가 없으면 실제 그룹 내 정렬을 확인한 assume_sorted=True 필요.</li>
<li>출력은 그룹 첫 등장 순서별로 묶인다. 원래 행 순서나 전체 시간순이 아닐 수 있다.</li>
<li>target_original_indices는 원래 입력의 정답 행 위치이며 DataFrame 인덱스 라벨/입력 끝 위치가 아니다.</li>
<li>그룹별 윈도 생성은 train/valid의 그룹 분리를 자동 수행하지 않는다. 새 그룹 일반화라면 window_groups로 별도 분할.</li>
<li>짧아서 윈도 없는 그룹은 건너뛰고 모든 그룹이 짧으면 오류. NaN 정답·불규칙 샘플 간격은 별도 처리.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-split_raw_time_then_window"><code>split_raw_time_then_window</code></h4>
<p>원시 시계열의 경계와 정답 가용성 기준으로 윈도를 훈련/검증에 배정한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>features</code></td>
<td>필수</td>
<td><code>series_df[['speed', 'temperature']].to_numpy(dtype=np.float32)</code></td>
<td>정답·ID를 제외한 시간순 원시 특성 행. (T,F), 1D (T,)도 한 특성으로 확장 가능. 시간 축 T는 전체 원시 행 수. 그룹 함수에서는 원래 행 대응 유지.</td>
</tr>
<tr>
<td><code>targets</code></td>
<td>필수</td>
<td><code>series_df[['fuel_use']].to_numpy(dtype=np.float32)</code></td>
<td>각 원시 행 시점의 정답 값 배열. (T,) 또는 (T,D), features와 같은 원시 행 길이. 미래 정답을 helper가 인덱스로 고르므로 이미 앞당긴 y를 다시 horizon만큼 이동하지 않는다.</td>
</tr>
<tr>
<td><code>train_ratio</code></td>
<td>필수</td>
<td><code>0.7</code></td>
<td>원시 시계열에서 훈련 쪽 경계를 놓을 비율. 0&lt;값&lt;1. 최종 윈도 행 비율과 다를 수 있다.</td>
</tr>
<tr>
<td><code>lookback</code></td>
<td>필수</td>
<td><code>20</code></td>
<td>문제에서 사용할 과거 시간 길이를 샘플 개수로 환산. 양의 정수. 100Hz에서 0.2초를 20개로 해석하는 문제 관례. 초 0.2를 그대로 넣지 않는다.</td>
</tr>
<tr>
<td><code>horizon</code></td>
<td>필수</td>
<td><code>40</code></td>
<td>마지막 입력 관측에서 목표까지의 간격을 행 개수로 환산. 0 이상 정수. 100Hz에서 0.4초 뒤는 40칸. 입력 시작이 아니라 마지막 입력에서 센다.</td>
</tr>
<tr>
<td><code>stride</code></td>
<td><code>1</code></td>
<td><code>1</code></td>
<td>윈도 시작 위치를 몇 행씩 이동할지 정한 값. 양의 정수. 윈도 helper에서는 초 단위가 아닌 시간순 행 간격.</td>
</tr>
<tr>
<td><code>gap</code></td>
<td><code>0</code></td>
<td><code>0</code></td>
<td>경계 주변에서 추가로 제외할 원시 행 간격. 0 이상 정수. 학습 경계는 cut−gap, 검증 시작 기준은 cut+gap이므로 양쪽에 적용된다.</td>
</tr>
<tr>
<td><code>assume_sorted</code></td>
<td><code>False</code></td>
<td><code>True</code></td>
<td>실제로 안정 시간순 정렬·행 대응을 확인한 뒤 명시하는 확인값. bool. True가 정렬을 실행해 주는 것은 아니다. 기본 False인 일반 윈도/분할/Lazy 호출은 오류를 낸다.</td>
</tr>
<tr>
<td><code>split_mode</code></td>
<td><code>'forecast_origin'</code></td>
<td><code>'forecast_origin'</code></td>
<td>실제 평가의 예측 기준이 무엇인지 명세에서 선택. 기본은 미래 예측의 입력 끝 기준. 'target'은 별도 배치 평가 계약용이며 미래 예측 누수를 자동 해결하지 않는다.</td>
</tr>
<tr>
<td><code>label_delay</code></td>
<td><code>0</code></td>
<td><code>2</code></td>
<td>정답 시점 이후 실제 정답이 알려지기까지의 추가 지연. 0 이상 정수. horizon과 별개의 원시 행 지연. 불규칙 시간은 timestamp로 별도 검사.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>(train,valid,cut). 각 train/valid는 (Xw,yw,target_idx) tuple, cut은 원시 행 경계 int.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-22">복사</button></div><pre id="template-code-07-22" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 22"><code class="language-python">import hdat_templates as h
import numpy as np
X = np.arange(40, dtype=np.float32).reshape(20, 2)
y = np.arange(20, dtype=np.float32)
train, valid, cut = h.split_raw_time_then_window(
    X, y, train_ratio=0.6, lookback=3, horizon=2,
    gap=0, label_delay=0, assume_sorted=True)
X_train, y_train, train_target = train
X_valid, y_valid, valid_target = valid
assert cut == 12
assert len(X_train) == 8 and len(X_valid) == 6
assert train_target.max() &lt; (valid_target - 2).min()
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>train 조건은 target_idx+label_delay&lt;cut−gap, 기본 valid 조건은 target_idx−horizon≥cut+gap.</li>
<li>target 모드는 valid target≥cut+gap이며, 미래 예측 기준에서 훈련 정답이 이미 알려져 있는지는 별도 보장해야 한다.</li>
<li>추가 gap=0이어도 horizon/label_delay 때문에 경계 부근 윈도가 제외될 수 있다.</li>
<li>그룹 인자를 받지 않는다. 한 시간순 시계열용이며 불규칙 시각은 실제 timestamp 조건이 필요하다.</li>
<li>이름과 달리 내부에서는 전체 윈도를 먼저 생성한 뒤 mask로 나눈다. 메모리 절약형이 아니다.</li>
<li>train 또는 valid가 비면 오류. 전처리 통계를 아직 fit하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-fit_scale_3d"><code>fit_scale_3d</code></h4>
<p>훈련 윈도의 모든 시점을 펼쳐 센서별 평균·표준편차를 학습한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>X_train</code></td>
<td>필수</td>
<td><code>X_train_windows</code></td>
<td>이미 train/valid를 나눈 뒤 훈련 윈도만. 수치 ndarray (N,T,F). 모델 입력에서 F가 마지막 축이어야 한다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>훈련된 StandardScaler. X 배열 자체는 변환하지 않는다.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-23">복사</button></div><pre id="template-code-07-23" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 23"><code class="language-python">import hdat_templates as h
import numpy as np
X_train = np.arange(12, dtype=np.float32).reshape(2, 3, 2)
scaler = h.fit_scale_3d(X_train)
assert np.allclose(scaler.mean_, [5.0, 6.0])
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>입력은 비어 있지 않은 (N,T,F)의 수치 ndarray. scaler는 마지막 F축별 통계를 배운다.</li>
<li>중첩 윈도의 같은 원시 관측이 반복 포함되면 통계도 반복 횟수로 가중된다. 원시 행을 한 번씩 fit한 통계와 다를 수 있다.</li>
<li>검증·테스트 윈도를 합쳐 fit하지 않는다. NaN/Inf 등 자료 유효성은 호출 전 점검.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-transform_scale_3d"><code>transform_scale_3d</code></h4>
<p>훈련에서 배운 센서별 기준을 3D 윈도에 적용하고 원래 shape로 돌린다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>X</code></td>
<td>필수</td>
<td><code>X_valid_windows</code></td>
<td>같은 센서 순서의 train/valid/test 윈도. NumPy (N,T,F). scaler가 학습한 F와 센서 순서가 일치해야 한다.</td>
</tr>
<tr>
<td><code>scaler</code></td>
<td>필수</td>
<td><code>x_scaler</code></td>
<td>h.fit_scale_3d(X_train)의 반환값. 훈련 특성 F개에 fit된 StandardScaler. 새로 만든 미학습 scaler가 아니다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>입력과 같은 (N,T,F) shape의 float32 ndarray.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-24">복사</button></div><pre id="template-code-07-24" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 24"><code class="language-python">import hdat_templates as h
import numpy as np
X_train = np.arange(12, dtype=np.float32).reshape(2, 3, 2)
X_valid = np.array([[[5., 6.], [7., 8.], [9., 10.]]], dtype=np.float32)
scaler = h.fit_scale_3d(X_train)
X_train_s = h.transform_scale_3d(X_train, scaler)
X_valid_s = h.transform_scale_3d(X_valid, scaler)
assert X_valid_s.shape == (1, 3, 2)
assert X_valid_s.dtype == np.float32
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>scaler는 fit_scale_3d로 훈련 F개 특성에 이미 fit되어 있어야 한다.</li>
<li>내부에서 3D 자체를 명시 검증하지 않으므로 호출자가 ndim=3과 같은 F축을 확인한다.</li>
<li>자료를 정렬/분할하거나 새 평균을 학습하지 않는다. feature 순서가 같아야 한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-validate_prediction_array"><code>validate_prediction_array</code></h4>
<p>예측 배열의 정확한 크기·수치형 여부·결측/무한값을 검사한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>pred</code></td>
<td>필수</td>
<td><code>test_pred</code></td>
<td>행 순서·단위·클래스 대응을 복원한 최종 제출 예측. 제출 계약의 shape/dtype. 함수가 의미·순서를 알아서 고치지 않는다.</td>
</tr>
<tr>
<td><code>expected_shape</code></td>
<td>필수</td>
<td><code>(len(test_df), 3)</code></td>
<td>문제의 제출 행 수와 출력 열 수에서 만든 tuple. 예: 한 값 (N,), 3개 값 (N,3). (N,)와 (N,1)은 엄격히 다르다.</td>
</tr>
<tr>
<td><code>allow_nan</code></td>
<td><code>False</code></td>
<td><code>False</code></td>
<td>문제에서 결측 허용을 명시했을 때만 변경. bool. 현 코드에서 True이면 NaN뿐 아니라 Inf 검사도 건너뛰므로 일반 제출에는 False 유지.</td>
</tr>
<tr>
<td><code>require_numeric</code></td>
<td><code>True</code></td>
<td><code>True</code></td>
<td>제출이 숫자만 허용하는지 확인. bool. False는 문자열 라벨을 허용하지만 열 의미·라벨 집합을 검증하지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>np.asarray(pred)의 결과 ndarray. dtype/shape를 임의로 고치지 않는다.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-25">복사</button></div><pre id="template-code-07-25" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 25"><code class="language-python">import hdat_templates as h
import numpy as np
pred = np.array([[1., 2., 3.], [4., 5., 6.]], dtype=np.float32)
checked = h.validate_prediction_array(pred, expected_shape=(2, 3))
assert checked.shape == (2, 3)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>기본은 수치형과 유한성 요구. expected_shape=(N,)와 (N,1)은 다르다.</li>
<li>allow_nan=True는 Inf까지 검사를 생략하므로 일반 제출에 사용하지 않는다.</li>
<li>np.number는 complex도 포함하고 bool은 일반 수치형 검사에 통과하지 않는다. 실수·정수·라벨 집합 조건은 추가 검사한다.</li>
<li>통과해도 행 순서·열 의미·단위·허용 라벨이 맞다는 증거는 아니다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-save_npy_submission"><code>save_npy_submission</code></h4>
<p>예측을 검사하고 NPY로 저장한 다음 다시 읽어 shape·근접한 값을 검사한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>pred</code></td>
<td>필수</td>
<td><code>test_pred</code></td>
<td>행 순서·단위·클래스 대응을 복원한 최종 제출 예측. 제출 계약의 shape/dtype. 함수가 의미·순서를 알아서 고치지 않는다.</td>
</tr>
<tr>
<td><code>path</code></td>
<td>필수</td>
<td><code>'Submission_problem.npy'</code></td>
<td>문제에서 정한 실제 파일명과 저장 위치. str 또는 pathlib.Path. 부모 폴더가 존재해야 하며 기존 같은 경로는 덮어쓴다.</td>
</tr>
<tr>
<td><code>expected_shape</code></td>
<td>필수</td>
<td><code>(len(test_df), 3)</code></td>
<td>문제의 제출 행 수와 출력 열 수에서 만든 tuple. 예: 한 값 (N,), 3개 값 (N,3). (N,)와 (N,1)은 엄격히 다르다.</td>
</tr>
<tr>
<td><code>dtype</code></td>
<td><code>None</code></td>
<td><code>np.float32</code></td>
<td>제출 명세의 정확한 원소 자료형. NumPy dtype 또는 None. None은 현재 dtype 유지. float→int는 자르기이므로 라벨 결정 대용이 아니다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>저장한 ndarray arr. 반환은 다시 읽은 loaded가 아니라 저장 전 검증·형변환 배열.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-26">복사</button></div><pre id="template-code-07-26" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 26"><code class="language-python">import hdat_templates as h
import numpy as np
from pathlib import Path
from tempfile import TemporaryDirectory
pred = np.array([0.2, 0.8], dtype=np.float64)
with TemporaryDirectory() as folder:  # 예시는 임시 파일만 만든다.
    path = Path(folder) / "Submission_problem.npy"
    saved = h.save_npy_submission(pred, path, expected_shape=(2,), dtype=np.float32)
    loaded = np.load(path, allow_pickle=False)
    assert loaded.dtype == np.float32
    assert np.array_equal(loaded, saved)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>연습용 helper이며 시험에 제공한 저장 셀이 있으면 그것을 따른다.</li>
<li>명시 dtype 변환 전후 유한성을 검사하므로 float32 overflow를 잡는다. 그러나 실수→정수 자르기의 의미 오류는 못 잡는다.</li>
<li>확장자가 정확히 .npy가 아니면 .npy를 뒤에 붙인다. 기존 파일은 덮어쓰며 폴더는 만들지 않는다.</li>
<li>재읽기는 np.allclose 기반이고 dtype 동일성/정확한 bitwise equality를 별도 확인하지 않는다.</li>
<li>NPY 저장이 노트북 저장·실제 제출 버튼 완료를 대신하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-save_csv_submission"><code>save_csv_submission</code></h4>
<p>제출 예시 표의 타깃 열에 예측을 넣고 CSV 저장·재읽기를 검사한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>sample_submission</code></td>
<td>필수</td>
<td><code>sample_submission_df</code></td>
<td>제공된 제출 예시 파일을 읽은 원래 표. DataFrame. ID·행 순서·출력 열이 기준이며 pred를 ID로 자동 join하지 않는다.</td>
</tr>
<tr>
<td><code>pred</code></td>
<td>필수</td>
<td><code>test_pred</code></td>
<td>행 순서·단위·클래스 대응을 복원한 최종 제출 예측. 제출 계약의 shape/dtype. 함수가 의미·순서를 알아서 고치지 않는다.</td>
</tr>
<tr>
<td><code>target_cols</code></td>
<td>필수</td>
<td><code>('fuel_use',)</code></td>
<td>문제 설명의 정답 열 이름과 출력 순서. 문자열의 list/tuple. 한 열 tuple에도 쉼표가 필요하다. ('fuel_use')는 문자열이다.</td>
</tr>
<tr>
<td><code>path</code></td>
<td>필수</td>
<td><code>'Submission.csv'</code></td>
<td>문제의 CSV 제출 파일명과 저장 위치. str 또는 Path. 부모 폴더가 있어야 하며 같은 경로는 덮어쓴다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>저장 전 작성한 DataFrame out. 원본 sample_submission은 복사하여 보존.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-27">복사</button></div><pre id="template-code-07-27" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 27"><code class="language-python">import hdat_templates as h
import numpy as np
import pandas as pd
from pathlib import Path
from tempfile import TemporaryDirectory
sample = pd.DataFrame({"id": [101, 102], "fault": [0, 0]})
pred = np.array([1, 0], dtype=np.int64)  # 101,102 행 순서
with TemporaryDirectory() as folder:
    path = Path(folder) / "Submission.csv"
    out = h.save_csv_submission(sample, pred, target_cols=["fault"], path=path)
    loaded = pd.read_csv(path)
    assert loaded["id"].tolist() == [101, 102]
    assert loaded["fault"].tolist() == [1, 0]
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>pred는 sample_submission의 현재 행 순서와 이미 대응해야 한다. ID로 자동 정렬/join하지 않는다.</li>
<li>한 열이면 (N,) 또는 (N,1), 여러 열이면 정확히 (N,D). 문자열 라벨도 지원하지만 허용 라벨 집합 검사는 없다.</li>
<li>NaN/숫자 Inf와 재읽은 열/행 수를 검사한다. 전체 ID 값·dtype·타깃 수치 일치까지 검증하지 않는다.</li>
<li>CSV 재읽기에서 'NA' 같은 문자열이나 앞자리 0인 ID가 다르게 해석될 수 있으므로 명세에 맞춘 추가 대조 필요.</li>
<li>index=False로 저장하며 기존 같은 파일을 덮어쓴다. 실제 제출 상태는 별도 확인한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-prepare_numpy_images"><code>prepare_numpy_images</code></h4>
<p>명시한 이미지 배열 layout을 NCHW float32로 변환하고 필요하면 255로 나눈다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>X</code></td>
<td>필수</td>
<td><code>images</code></td>
<td>파일/배열을 읽고 이미지 batch로 모은 실제 픽셀 배열. 3D (N,H,W) 흑백 batch 또는 layout에 맞는 4D batch. 단일 RGB HWC는 먼저 배치 축 추가.</td>
</tr>
<tr>
<td><code>layout</code></td>
<td>필수</td>
<td><code>'NHWC'</code></td>
<td>이미지 배열의 실제 축 순서를 shape와 자료 설명으로 확인. 'NHWC'|'NCHW' 필수. 3D 입력은 단일 RGB가 아니라 흑백 배치 (N,H,W)로 해석한다.</td>
</tr>
<tr>
<td><code>divide_255</code></td>
<td>필수</td>
<td><code>True</code></td>
<td>입력 픽셀이 아직 0..255 척도인지 실제 값으로 확인. bool 필수. 이미 0..1 또는 표준화한 값이면 False. float dtype 자체는 이미 나눴다는 증거가 아니다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>NumPy float32 배열 (N,C,H,W), C는 1·3·4 중 하나.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-28">복사</button></div><pre id="template-code-07-28" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 28"><code class="language-python">import hdat_templates as h
import numpy as np
images = np.zeros((2, 16, 20, 3), dtype=np.uint8)  # N,H,W,C
images[..., 0] = 255
prepared = h.prepare_numpy_images(images, layout="NHWC", divide_255=True)
assert prepared.shape == (2, 3, 16, 20)
assert prepared.dtype == np.float32
assert prepared[:, 0].max() == 1.0
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>layout과 divide_255에 기본값이 없다. 실제 축 순서·픽셀 척도를 보고 둘 다 지정한다.</li>
<li>3D는 흑백 배치 (N,H,W)로 해석한다. 단일 RGB (H,W,3)에는 X[None,...]로 배치 축을 먼저 추가한다.</li>
<li>RGB 변환·알파 제거·resize·채널별 mean/std 정규화·증강은 하지 않는다.</li>
<li>입력이 float라도 여전히 0..255일 수 있다. 이미 0..1인 배열을 다시 나누지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-print_shapes"><code>print_shapes</code></h4>
<p>여러 배열을 이름과 함께 shape·dtype로 출력한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>**arrays</code></td>
<td><code>{}</code></td>
<td><code>X_train=X_train_s, y_train=y_train, pred=test_pred</code></td>
<td>이름을 붙여 점검할 현재 배열들. 키워드 인자 여러 개. NumPy로 변환할 수 있는 배열. GPU Tensor·dict/sparse 등은 따로 확인.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>None. 화면에 점검 문자열을 출력.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-07-29">복사</button></div><pre id="template-code-07-29" tabindex="0" aria-label="입력·전처리·분할·저장 함수 사전 코드 29"><code class="language-python">import hdat_templates as h
import numpy as np
X_train = np.zeros((8, 6), dtype=np.float32)
y_train = np.zeros((8, 1), dtype=np.float32)
pred = np.zeros((3, 1), dtype=np.float32)
h.print_shapes(X_train=X_train, y_train=y_train, pred=pred)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>키워드 이름은 설명용으로 원하는 이름을 붙인다. h.print_shapes(X_train=X)는 문자열 'X_train'을 배열 이름으로 출력.</li>
<li>검사 실패를 raise하지 않고 유한성/축 의미/행 대응을 검증하지 않는다.</li>
<li>GPU Tensor는 먼저 detach().cpu().numpy() 등으로 변환해야 한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
1f:["$","div",null,{"className":"lesson-content playbook-content template-content","dangerouslySetInnerHTML":{"__html":"$25"}}]
20:["$","div",null,{"className":"playbook-bottom","children":[["$","a",null,{"href":"/hdat-ds-study-hub/templates/09/","children":"학습 함수의 모든 인자"}],["$","a",null,{"href":"/hdat-ds-study-hub/playbook/","children":"문제 유형별 풀이"}],["$","a",null,{"href":"/hdat-ds-study-hub/downloads/hdat-template-guide.html","download":true,"children":"오프라인 매뉴얼 ↓"}]]}]
21:["$","nav",null,{"className":"lesson-pager","aria-label":"호출 매뉴얼 앞뒤 이동","children":[["$","a",null,{"href":"/hdat-ds-study-hub/templates/06/","children":["← ","지표·손실·저장 인자를 내 문제에 맞추기"]}],["$","a",null,{"href":"/hdat-ds-study-hub/templates/08/","children":["모델을 만드는 인자 사전"," →"]}]]}]
22:["$","footer",null,{"className":"site-footer","children":[["$","div",null,{"className":"shell footer-grid","children":[["$","div",null,{"children":[["$","strong",null,{"children":"HDAT DS · PyTorch Study Room"}],["$","p",null,{"children":"Python을 처음 배우는 사람도 데이터 분석부터 차근차근 공부할 수 있도록 만든 개인 학습 자료입니다."}]]}],["$","nav",null,{"aria-label":"하단 메뉴","children":[["$","a",null,{"href":"/hdat-ds-study-hub/start/","children":"Python 기초 10강"}],["$","a",null,{"href":"/hdat-ds-study-hub/learn/","children":"HDAT 본과정 34강"}],["$","a",null,{"href":"/hdat-ds-study-hub/practice/","children":"실전 연습"}],["$","a",null,{"href":"/hdat-ds-study-hub/resources/","children":"자료실"}],["$","a",null,{"href":"/hdat-ds-study-hub/cheatsheet.html","children":"로컬 치트시트"}]]}]]}],["$","p",null,{"className":"legal shell","children":"이 사이트는 개인 학습용 비공식 자료이며 현대자동차그룹 또는 현대엔지비의 공식 사이트가 아닙니다. 시험 당일 문제 지시, 제공 스켈레톤, 공식 규정과 감독관 안내가 항상 우선합니다."}]]}]
