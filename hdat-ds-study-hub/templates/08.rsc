2:I["8c0f216c4604",[],"Children",1]
3:I["593f344dc510",[],"GlobalErrorBoundary",1]
4:I["0b874ad30386",[],"default",1]
5:I["593f344dc510",[],"ErrorBoundary",1]
6:I["15c18cfaeeff",[],"LayoutSegmentProvider",1]
7:I["8c0f216c4604",[],"Slot",1]
8:I["9276801271d6",[],"AppRouterScrollTarget",1]
9:I["593f344dc510",[],"RedirectBoundary",1]
:HL["/hdat-ds-study-hub/_next/static/css/index.COtfHe_8.css","style"     ]
0:{"__route":"route:/templates/08","__interceptionContext":null,"__layoutIds":["layout:/"],"__rootLayout":"/","__sourcePage":"/templates/[chapter]/page","page:/templates/08":"$L1","layout:/":[[[["$","link","css:/hdat-ds-study-hub/_next/static/css/index.COtfHe_8.css",{"rel":"stylesheet","precedence":"vite-rsc/importer-resources","href":"/hdat-ds-study-hub/_next/static/css/index.COtfHe_8.css","data-rsc-css-href":"/hdat-ds-study-hub/_next/static/css/index.COtfHe_8.css"}],"$undefined"],["$","html",null,{"lang":"ko","children":[["$","head",null,{"children":[["$","meta",null,{"name":"theme-color","content":"#07152b"}],["$","meta",null,{"name":"description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"name":"application-name","content":"HDAT DS Study Hub"}],["$","meta",null,{"name":"author","content":"HDAT DS Study Hub"}],["$","meta",null,{"name":"robots","content":"index, follow"}],["$","meta",null,{"property":"og:type","content":"website"}],["$","meta",null,{"property":"og:locale","content":"ko_KR"}],["$","meta",null,{"property":"og:url","content":"https://markshincuhk.github.io/hdat-ds-study-hub"}],["$","meta",null,{"property":"og:site_name","content":"HDAT DS Study Hub"}],["$","meta",null,{"property":"og:title","content":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}],["$","meta",null,{"property":"og:description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"property":"og:image","content":"https://markshincuhk.github.io/hdat-ds-study-hub/og.png"}],["$","meta",null,{"property":"og:image:width","content":"1726"}],["$","meta",null,{"property":"og:image:height","content":"911"}],["$","meta",null,{"property":"og:image:alt","content":"Python 기초부터 HDAT DS까지 배우는 PyTorch 학습실"}],["$","meta",null,{"name":"twitter:card","content":"summary_large_image"}],["$","meta",null,{"name":"twitter:title","content":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}],["$","meta",null,{"name":"twitter:description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"name":"twitter:image","content":"https://markshincuhk.github.io/hdat-ds-study-hub/og.png"}],["$","link",null,{"rel":"icon","href":"/hdat-ds-study-hub/favicon.svg","type":"image/svg+xml"}],["$","title",null,{"children":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}]]}],["$","body",null,{"children":["$","$L2",null,{}]}]]}]],null],"route:/templates/08":[[["$","meta",null,{"charSet":"utf-8"}],[["$","title","0",{"children":"모델을 만드는 인자 사전 | HDAT DS"}]],[["$","meta","0",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]],["$","$L3",null,{"fallback":"$4","children":["$","$L5",null,{"fallback":"$4","children":["$","$L6",null,{"providerId":"layout:/","segmentMap":{"children":["templates","08"]},"children":["$","$L7",null,{"id":"layout:/","parallelSlots":"$undefined","children":["$","$L8",null,{"children":["$","$L9",null,{"children":[["$","$L6",null,{"providerId":"page:/templates/08","segmentMap":{"children":["__PAGE__"]},"children":["$","$L7",null,{"id":"page:/templates/08"}]}],null]}]}]}]}]}]}],null,null],"__layoutFlags":{"layout:/":"s"},"__artifactCompatibility":{"schemaVersion":1,"graphVersion":"app-route-graph:467b84659031f790","deploymentVersion":"b09e4206-5d79-45c5-90be-eef7c466cf6b","appElementsSchemaVersion":1,"rscPayloadSchemaVersion":1,"rootBoundaryId":"/","renderEpoch":null},"__renderObservation":{"schemaVersion":1,"output":{"kind":"app-rsc","mountedSlotsFingerprint":null,"renderEpoch":null,"rootBoundaryId":"/","routeId":"route:/templates/08"},"completeness":"partial","boundaryOutcome":{"kind":"unknown"},"requestApis":[{"kind":"connection","status":"unknown"},{"kind":"cookies","status":"unknown"},{"kind":"draftMode","status":"unknown"},{"kind":"headers","status":"unknown"},{"kind":"params","status":"observed"},{"kind":"searchParams","status":"unknown"}],"dynamicFetches":[],"cacheTags":["/templates/08","_N_T_/layout","_N_T_/templates/08","_N_T_/templates/[chapter]/layout","_N_T_/templates/[chapter]/page","_N_T_/templates/layout"],"pathTags":["/templates/08"],"cacheability":"unknown","downgrade":{"target":"freshRender","reasons":[{"code":"CP_DOWNGRADE_CACHEABILITY_UNKNOWN","target":"freshRender"},{"code":"CP_DOWNGRADE_INCOMPLETE_OBSERVATION","completeness":"partial","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"connection","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"cookies","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"draftMode","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"headers","target":"freshRender"},{"code":"CP_DOWNGRADE_PUBLIC_REQUEST_API","requestApi":"params","target":"publicVariant"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"searchParams","target":"freshRender"}],"fallback":{"kind":"breakerFallback","code":"CP_PRIVATE_DYNAMIC_DOWNGRADE","mode":"renderFresh","scope":"affectedOutput","fields":{"reasonCodes":["CP_DOWNGRADE_CACHEABILITY_UNKNOWN","CP_DOWNGRADE_INCOMPLETE_OBSERVATION","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_PUBLIC_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API"],"target":"freshRender"}},"isPublicCacheCandidate":false}}}
a:I["326fe58ccdc4",[],"default",1]
b:I["66e71be26559",[],"default",1]
c:I["ad205a1e87e8",[],"default",1]
d:T9e66,<p>모델은 이름만 고르는 것이 아니라 데이터에 맞는 크기로 만들어야 합니다. 아래 예제의 model(x)는 출력 모양만 확인합니다. 학습이 끝났다는 뜻은 아닙니다. 지정 구조 문제에서는 이 기본 모델과 문제의 층 구성이 같은지 별도로 대조하세요.</p>
<h4 id="api-MLP"><code>MLP</code></h4>
<p>표 데이터 또는 평탄화한 한 샘플을 입력받는 신경망을 만든다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>n_features</code></td>
<td>필수</td>
<td><code>X_train_s.shape[1]</code></td>
<td>전처리된 2D 입력 폭. 3D 이상을 넣으면 한 샘플 flatten 후의 원소 수. 2D (N,F)면 F. (N,T,F)를 직접 넣으면 T×F로 지정해야 한다.</td>
</tr>
<tr>
<td><code>out_dim</code></td>
<td>필수</td>
<td><code>1</code></td>
<td>문제의 출력 계약에서 결정한 출력 수. 회귀 D, 이진 1, 다중분류 C, 다중라벨 K. 정답의 원소 수 N이 아니다.</td>
</tr>
<tr>
<td><code>hidden</code></td>
<td><code>(128, 64)</code></td>
<td><code>(128, 64)</code></td>
<td>문제 명세 또는 작은 검증에서 고른 MLP 은닉층 폭. 양의 정수 시퀀스. (64,)가 한 층이며 64 단독은 시퀀스가 아니다. 빈 tuple은 Linear 한 층.</td>
</tr>
<tr>
<td><code>dropout</code></td>
<td><code>0.1</code></td>
<td><code>0.1</code></td>
<td>문제 명세 또는 검증에서 정한 drop 비율. 0≤p≤1 범위 실수. 보통 0.1은 10%이며 10을 넣지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>model(x)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,F). 3D 이상이면 flatten(1) 후 폭이 n_features와 같아야 한다.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: MLP 인스턴스. model(xb): float Tensor (B,out_dim).</p>
<p><strong>반환값</strong></p>
<p>생성: MLP 인스턴스. model(xb): float Tensor (B,out_dim).</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-1">복사</button></div><pre id="template-code-08-1" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 1"><code class="language-python">import hdat_templates as h
import numpy as np
import torch
X = np.zeros((4, 6), dtype=np.float32)  # 샘플 4개, 전처리된 특성 6개
model = h.MLP(n_features=X.shape[1], out_dim=1, hidden=(16, 8))
raw = model(torch.as_tensor(X))
assert raw.shape == (4, 1)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>n_features는 전처리 이후의 폭이다. 원-핫 표현 후에는 원본 열 개수와 다르다.</li>
<li>3D 이상 x는 flatten(1)하므로 시계열 MLP라면 n_features=T×F이어야 한다.</li>
<li>마지막에 sigmoid/softmax가 없다. 회귀 연속값·분류 raw logits로 해석한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-CNN1D"><code>CNN1D</code></h4>
<p>시간 윈도 (B,T,F)를 받아 시간축 합성곱으로 예측한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>n_features</code></td>
<td>필수</td>
<td><code>X_train_s.shape[-1]</code></td>
<td>전처리가 끝난 실제 모델 입력 배열의 특성 수. 양의 정수. MLP 2D면 shape[1]; CNN1D/RNN/Transformer (N,T,F)면 shape[2]. 원본 CSV 전체 열 수나 배치 수가 아니다.</td>
</tr>
<tr>
<td><code>out_dim</code></td>
<td>필수</td>
<td><code>1</code></td>
<td>문제의 출력 계약에서 결정한 출력 수. 회귀 D, 이진 1, 다중분류 C, 다중라벨 K. 정답의 원소 수 N이 아니다.</td>
</tr>
<tr>
<td><code>channels</code></td>
<td><code>(64, 128)</code></td>
<td><code>(64, 128)</code></td>
<td>문제 명세 또는 검증에서 정한 CNN1D 두 층 채널 수. 양의 정수 정확히 2개. 센서 개수 F와는 별개의 학습 특징 채널.</td>
</tr>
<tr>
<td><code>dropout</code></td>
<td><code>0.1</code></td>
<td><code>0.1</code></td>
<td>문제 명세 또는 검증에서 정한 drop 비율. 0≤p≤1 범위 실수. 보통 0.1은 10%이며 10을 넣지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>model(x)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,T,F). 내부에서 (B,F,T)로 전치하므로 미리 전치하지 않는다.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: CNN1D 인스턴스. model(xb): (B,out_dim) 실수 Tensor.</p>
<p><strong>반환값</strong></p>
<p>생성: CNN1D 인스턴스. model(xb): (B,out_dim) 실수 Tensor.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-2">복사</button></div><pre id="template-code-08-2" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 2"><code class="language-python">import hdat_templates as h
import numpy as np
import torch
X = np.zeros((4, 20, 6), dtype=np.float32)
model = h.CNN1D(n_features=X.shape[-1], out_dim=3, channels=(8, 16))
raw = model(torch.as_tensor(X))
assert raw.shape == (4, 3)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>forward 안에서 (B,T,F)→(B,F,T)로 전치한다. 호출 전에 이미 전치하면 축을 다시 뒤집게 된다.</li>
<li>channels는 정확히 두 값. kernel/normalization 구조는 고정되어 있어 명세형 Process 문제에 무수정 사용하면 안 된다.</li>
<li>전역 평균 풀링을 사용하므로 마지막 시점 정보를 특별히 강조하는 구조는 아니다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-SequenceRNN"><code>SequenceRNN</code></h4>
<p>고정 길이 시계열을 GRU/LSTM으로 읽고 마지막 층 은닉 상태로 한 번 예측한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>n_features</code></td>
<td>필수</td>
<td><code>X_train_s.shape[-1]</code></td>
<td>전처리가 끝난 실제 모델 입력 배열의 특성 수. 양의 정수. MLP 2D면 shape[1]; CNN1D/RNN/Transformer (N,T,F)면 shape[2]. 원본 CSV 전체 열 수나 배치 수가 아니다.</td>
</tr>
<tr>
<td><code>out_dim</code></td>
<td>필수</td>
<td><code>1</code></td>
<td>문제의 출력 계약에서 결정한 출력 수. 회귀 D, 이진 1, 다중분류 C, 다중라벨 K. 정답의 원소 수 N이 아니다.</td>
</tr>
<tr>
<td><code>hidden_size</code></td>
<td><code>64</code></td>
<td><code>64</code></td>
<td>RNN 한 방향의 은닉 상태 폭. 양의 정수. bidirectional=True면 최종 표현 폭은 2×hidden_size.</td>
</tr>
<tr>
<td><code>num_layers</code></td>
<td><code>1</code></td>
<td><code>2</code></td>
<td>겹칠 순환층 또는 Transformer encoder 층 수. 양의 정수. 여러 시점 T와 다르다.</td>
</tr>
<tr>
<td><code>kind</code></td>
<td><code>'gru'</code></td>
<td><code>'lstm'</code></td>
<td>문제 명세가 요구한 순환 셀 종류. 'gru'|'lstm'만 가능.</td>
</tr>
<tr>
<td><code>bidirectional</code></td>
<td><code>False</code></td>
<td><code>False</code></td>
<td>전체 입력 구간을 양방향으로 읽어도 되는 과제인지 선택. bool. 같은 입력 구간 안 과거·역방향을 읽는다. 실시간 시점별 미래 차단을 자동 보장하지 않는다.</td>
</tr>
<tr>
<td><code>dropout</code></td>
<td><code>0.0</code></td>
<td><code>0.1</code></td>
<td>문제 명세 또는 검증에서 정한 drop 비율. 0≤p≤1 범위 실수. 보통 0.1은 10%이며 10을 넣지 않는다.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>model(x)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,T,F), 패딩 없는 고정 길이 입력.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: SequenceRNN 인스턴스. model(xb): (B,out_dim) 실수 Tensor.</p>
<p><strong>반환값</strong></p>
<p>생성: SequenceRNN 인스턴스. model(xb): (B,out_dim) 실수 Tensor.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-3">복사</button></div><pre id="template-code-08-3" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 3"><code class="language-python">import hdat_templates as h
import numpy as np
import torch
X = np.zeros((4, 20, 6), dtype=np.float32)
model = h.SequenceRNN(n_features=X.shape[-1], out_dim=1,
                    hidden_size=8, num_layers=2, kind="lstm",
                    bidirectional=False, dropout=0.1)
raw = model(torch.as_tensor(X))
assert raw.shape == (4, 1)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>n_features=입력 F, hidden_size=한 방향 상태 폭. 양방향은 마지막 층의 두 방향 상태를 연결한다.</li>
<li>num_layers=1이면 전달한 dropout도 내부에서 0으로 바뀐다.</li>
<li>패딩/가변 길이 packing을 지원하지 않는다. 마지막 패딩이 들어간 batch에 그대로 사용하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-SmallImageCNN"><code>SmallImageCNN</code></h4>
<p>NCHW 이미지 배치를 읽어 샘플별 연속값 또는 클래스 점수를 출력한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>in_channels</code></td>
<td>필수</td>
<td><code>X_images.shape[1]</code></td>
<td>NCHW 입력 텐서의 실제 채널 축 크기. 양의 정수. RGB 3, 흑백 1. H/W나 배치 수가 아니다.</td>
</tr>
<tr>
<td><code>out_dim</code></td>
<td>필수</td>
<td><code>1</code></td>
<td>문제의 출력 계약에서 결정한 출력 수. 회귀 D, 이진 1, 다중분류 C, 다중라벨 K. 정답의 원소 수 N이 아니다.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>model(x)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,in_channels,H,W), H/W 각각 최소 4.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: SmallImageCNN 인스턴스. model(xb): (B,out_dim) float Tensor.</p>
<p><strong>반환값</strong></p>
<p>생성: SmallImageCNN 인스턴스. model(xb): (B,out_dim) float Tensor.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-4">복사</button></div><pre id="template-code-08-4" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 4"><code class="language-python">import hdat_templates as h
import numpy as np
import torch
images = np.zeros((2, 3, 16, 20), dtype=np.float32)  # 이미 NCHW
model = h.SmallImageCNN(in_channels=images.shape[1], out_dim=4)
raw = model(torch.as_tensor(images))
assert raw.shape == (2, 4)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>in_channels는 준비된 배열 shape[1]에서 확인한다. RGB=3, 흑백=1.</li>
<li>MaxPool2d(2)가 두 번 있어 H/W는 최소 4여야 한다. 'adaptive이므로 어떤 양의 크기든 가능'은 틀리다.</li>
<li>다른 크기의 이미지는 한 일반 batch로 stack하기 전에 크기를 맞춰야 한다.</li>
<li>마지막 sigmoid/softmax 없음. 강제된 CNN 명세라면 구조를 그대로 사용하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-ResidualBlock2D"><code>ResidualBlock2D</code></h4>
<p>주 분기와 지름길을 같은 크기로 맞춰 더하는 2D 특징 블록을 만든다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>in_channels</code></td>
<td>필수</td>
<td><code>X_images.shape[1]</code></td>
<td>NCHW 입력 텐서의 실제 채널 축 크기. 양의 정수. RGB 3, 흑백 1. H/W나 배치 수가 아니다.</td>
</tr>
<tr>
<td><code>out_channels</code></td>
<td>필수</td>
<td><code>64</code></td>
<td>다음 잔차 블록이 만들 특징 채널 수. 양의 정수. 분류 클래스 수가 아닌 내부 특징 채널.</td>
</tr>
<tr>
<td><code>stride</code></td>
<td><code>1</code></td>
<td><code>2</code></td>
<td>공간 해상도를 유지할지 줄일지 정한 블록 설정. 양의 정수. 1이면 해상도 유지, 2이면 대략 절반이며 두 분기에 함께 적용된다.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>model(x)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,in_channels,H,W), BN 훈련의 충분한 표본/공간 크기 확인.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: ResidualBlock2D. model(xb): (B,out_channels,ceil(H/stride),ceil(W/stride)).</p>
<p><strong>반환값</strong></p>
<p>생성: ResidualBlock2D. model(xb): (B,out_channels,ceil(H/stride),ceil(W/stride)).</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-5">복사</button></div><pre id="template-code-08-5" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 5"><code class="language-python">import hdat_templates as h
import torch
x = torch.zeros(2, 3, 16, 20)
block = h.ResidualBlock2D(in_channels=x.shape[1], out_channels=8, stride=2)
features = block(x)
assert features.shape == (2, 8, 8, 10)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>최종 분류기가 아니라 중간 특징 블록이므로 out_channels를 클래스 수로 혼동하지 않는다.</li>
<li>채널 또는 stride가 바뀌면 1×1 projection이 생긴다. stride는 양의 정수.</li>
<li>BN 훈련은 채널당 통계가 가능한 값 개수가 필요하다. 단일 B=1,H=W=1은 별도 eval 테스트가 필요하다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-TransformerSequenceModel"><code>TransformerSequenceModel</code></h4>
<p>시퀀스 특성을 내부 폭으로 투영하고 위치 정보·자기어텐션·평균 풀링으로 예측한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>n_features</code></td>
<td>필수</td>
<td><code>X_train_s.shape[-1]</code></td>
<td>전처리가 끝난 실제 모델 입력 배열의 특성 수. 양의 정수. MLP 2D면 shape[1]; CNN1D/RNN/Transformer (N,T,F)면 shape[2]. 원본 CSV 전체 열 수나 배치 수가 아니다.</td>
</tr>
<tr>
<td><code>out_dim</code></td>
<td>필수</td>
<td><code>1</code></td>
<td>문제의 출력 계약에서 결정한 출력 수. 회귀 D, 이진 1, 다중분류 C, 다중라벨 K. 정답의 원소 수 N이 아니다.</td>
</tr>
<tr>
<td><code>d_model</code></td>
<td><code>64</code></td>
<td><code>64</code></td>
<td>입력 특성을 투영할 Transformer 내부 폭. 양의 정수, nhead로 나누어떨어져야 한다.</td>
</tr>
<tr>
<td><code>nhead</code></td>
<td><code>4</code></td>
<td><code>4</code></td>
<td>어텐션 head 개수. 양의 정수. d_model%nhead==0; 출력 클래스 수와 무관.</td>
</tr>
<tr>
<td><code>num_layers</code></td>
<td><code>2</code></td>
<td><code>2</code></td>
<td>겹칠 순환층 또는 Transformer encoder 층 수. 양의 정수. 여러 시점 T와 다르다.</td>
</tr>
<tr>
<td><code>dim_feedforward</code></td>
<td><code>128</code></td>
<td><code>128</code></td>
<td>Transformer 내부 MLP의 은닉 폭. 양의 정수. 입력 F·시퀀스 T와 별도 설정.</td>
</tr>
<tr>
<td><code>dropout</code></td>
<td><code>0.1</code></td>
<td><code>0.1</code></td>
<td>문제 명세 또는 검증에서 정한 drop 비율. 0≤p≤1 범위 실수. 보통 0.1은 10%이며 10을 넣지 않는다.</td>
</tr>
<tr>
<td><code>max_len</code></td>
<td><code>512</code></td>
<td><code>512</code></td>
<td>허용할 가장 긴 입력 시퀀스 길이. 양의 정수이며 모든 배치 T≤max_len. 위치 파라미터 크기를 정한다.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>model(x, padding_mask=padding_mask)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,T,n_features), 1≤T≤max_len.</td>
</tr>
<tr>
<td><code>padding_mask</code></td>
<td><code>None</code></td>
<td><code>torch.tensor([[False, False, True]])</code></td>
<td>실제 길이와 padding 위치로 만든 마스크. bool (B,T), True가 무시할 padding. x와 같은 device. 샘플마다 하나 이상 False 필요.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: TransformerSequenceModel. model(x,padding_mask=None): (B,out_dim) 실수 Tensor.</p>
<p><strong>반환값</strong></p>
<p>생성: TransformerSequenceModel. model(x,padding_mask=None): (B,out_dim) 실수 Tensor.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-6">복사</button></div><pre id="template-code-08-6" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 6"><code class="language-python">import hdat_templates as h
import torch
x = torch.zeros(2, 5, 3)
padding_mask = torch.tensor([[False, False, False, False, False],
                             [False, False, False, True, True]])
model = h.TransformerSequenceModel(n_features=x.shape[-1], out_dim=1,
                                  d_model=8, nhead=2, num_layers=1,
                                  dim_feedforward=16, max_len=5)
raw = model(x, padding_mask=padding_mask)
assert raw.shape == (2, 1)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>forward x는 (B,T,F), padding_mask는 선택 bool (B,T): True가 무시할 패딩, False가 실제 관측.</li>
<li>d_model은 nhead로 나누어져야 하고 T≤max_len. 각 샘플에 실제 관측이 적어도 하나는 있어야 한다.</li>
<li>padding_mask는 인과 마스크가 아니다. 전체 입력 구간 내부를 양방향으로 보며 토큰별 미래 차단 기능은 없다.</li>
<li>공통 train_torch_model은 model(xb)만 호출하므로 mask가 필요한 가변 길이 배치에는 전용 루프를 사용한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-Autoencoder"><code>Autoencoder</code></h4>
<p>입력을 작은 잠재 벡터로 줄인 뒤 원래 특성으로 복원한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>input_dim</code></td>
<td>필수</td>
<td><code>X_train_flat.shape[1]</code></td>
<td>평탄화·전처리한 한 샘플의 입력 특성 수. 양의 정수 F. AE/VAE/Discriminator는 기본 (B,F) 입력이며 자동 flatten하지 않는다.</td>
</tr>
<tr>
<td><code>latent_dim</code></td>
<td><code>16</code></td>
<td><code>8</code></td>
<td>검증 또는 문제 명세로 정한 잠재 표현의 폭. 양의 정수. 데이터 클래스 수·원시 행 수에서 자동 계산하는 값이 아니다.</td>
</tr>
<tr>
<td><code>hidden_dim</code></td>
<td><code>64</code></td>
<td><code>64</code></td>
<td>문제 명세 또는 작은 검증으로 고른 내부 은닉 폭. 양의 정수. Discriminator는 hidden_dim//2 층을 쓰므로 최소 2 권장.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>model(x)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,input_dim). 입력을 미리 평탄화한다.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: Autoencoder 인스턴스. model(xb): xb와 같은 (B,input_dim) 복원 Tensor.</p>
<p><strong>반환값</strong></p>
<p>생성: Autoencoder 인스턴스. model(xb): xb와 같은 (B,input_dim) 복원 Tensor.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-7">복사</button></div><pre id="template-code-08-7" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 7"><code class="language-python">import hdat_templates as h
import numpy as np
import torch
X = np.array([[0., 1., 2.], [1., 2., 3.]], dtype=np.float32)
model = h.Autoencoder(input_dim=X.shape[1], latent_dim=2, hidden_dim=8)
reconstruction = model(torch.as_tensor(X))
assert reconstruction.shape == X.shape
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>기본 사용은 2D (B,F); 이미지/윈도는 호출 전에 한 샘플씩 평탄화한다.</li>
<li>공통 학습 루프를 쓰려면 task='regression', loader의 y=X로 같은 복원 목표를 만들어야 한다.</li>
<li>h.predict_torch(task='regression')의 결과는 이상 라벨이 아니라 복원값이다. 샘플별 오차와 정상 검증 임계값은 별도 계산한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-VariationalAutoencoder"><code>VariationalAutoencoder</code></h4>
<p>입력별 잠재 평균·로그분산을 만들고 재매개화한 잠재 변수로 복원한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>input_dim</code></td>
<td>필수</td>
<td><code>X_train_flat.shape[1]</code></td>
<td>평탄화·전처리한 한 샘플의 입력 특성 수. 양의 정수 F. AE/VAE/Discriminator는 기본 (B,F) 입력이며 자동 flatten하지 않는다.</td>
</tr>
<tr>
<td><code>latent_dim</code></td>
<td><code>8</code></td>
<td><code>8</code></td>
<td>검증 또는 문제 명세로 정한 잠재 표현의 폭. 양의 정수. 데이터 클래스 수·원시 행 수에서 자동 계산하는 값이 아니다.</td>
</tr>
<tr>
<td><code>hidden_dim</code></td>
<td><code>64</code></td>
<td><code>64</code></td>
<td>문제 명세 또는 작은 검증으로 고른 내부 은닉 폭. 양의 정수. Discriminator는 hidden_dim//2 층을 쓰므로 최소 2 권장.</td>
</tr>
</tbody></table></div>
<p><strong>reparameterize 호출 인자</strong></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>mu</code></td>
<td>필수</td>
<td><code>mu</code></td>
<td>VAE forward가 반환한 잠재 평균. (B,Z) 실수 Tensor. 모델 객체·원시 입력 평균이 아니다.</td>
</tr>
<tr>
<td><code>logvar</code></td>
<td>필수</td>
<td><code>logvar</code></td>
<td>VAE forward가 반환한 잠재 로그분산. mu와 같은 (B,Z) 실수 Tensor. 분산/표준편차를 그대로 넣지 않는다.</td>
</tr>
</tbody></table></div>
<p>반환: (B,Z) Tensor. train은 잡음 표본, eval은 mu.</p>
<p><strong>forward 호출 인자</strong> — <code>model(x)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,input_dim). 입력을 미리 평탄화한다.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: VariationalAutoencoder 인스턴스. model(xb)는 (reconstruction,mu,logvar) tuple; shape는 (B,F),(B,Z),(B,Z).</p>
<p><strong>반환값</strong></p>
<p>생성: VariationalAutoencoder 인스턴스. model(xb)는 (reconstruction,mu,logvar) tuple; shape는 (B,F),(B,Z),(B,Z).</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-8">복사</button></div><pre id="template-code-08-8" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 8"><code class="language-python">import hdat_templates as h
import numpy as np
import torch
X = np.zeros((4, 6), dtype=np.float32)
model = h.VariationalAutoencoder(input_dim=X.shape[1], latent_dim=2, hidden_dim=8)
model.eval()
with torch.inference_mode():
    reconstruction, mu, logvar = model(torch.as_tensor(X))
assert reconstruction.shape == (4, 6)
assert mu.shape == logvar.shape == (4, 2)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>train 모드의 reparameterize(mu,logvar)는 mu+exp(logvar/2)×무작위 잡음, eval 모드는 mu를 반환한다.</li>
<li>공통 train_torch_model/predict_torch는 Tensor 하나를 기대하므로 tuple 출력 VAE에 그대로 쓸 수 없다.</li>
<li>vae_loss와 전용 optimizer 루프를 쓴다. eval의 평균 잠재 복원은 Monte Carlo ELBO 평가와 다르다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-vae_loss"><code>vae_loss</code></h4>
<p>VAE의 복원 오차와 KL 규제를 합친 학습 손실을 계산한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>reconstruction</code></td>
<td>필수</td>
<td><code>reconstruction</code></td>
<td>VAE forward가 반환한 복원 텐서. x와 같은 (B,F), 실수 Tensor. tuple 전체를 넣지 않는다.</td>
</tr>
<tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>현재 배치의 입력 텐서. 실수 Tensor. VAE loss에서는 reconstruction과 같은 (B,F), 같은 device.</td>
</tr>
<tr>
<td><code>mu</code></td>
<td>필수</td>
<td><code>mu</code></td>
<td>VAE forward가 반환한 잠재 평균. (B,Z) 실수 Tensor. 모델 객체·원시 입력 평균이 아니다.</td>
</tr>
<tr>
<td><code>logvar</code></td>
<td>필수</td>
<td><code>logvar</code></td>
<td>VAE forward가 반환한 잠재 로그분산. mu와 같은 (B,Z) 실수 Tensor. 분산/표준편차를 그대로 넣지 않는다.</td>
</tr>
<tr>
<td><code>beta</code></td>
<td><code>1.0</code></td>
<td><code>1.0</code></td>
<td>복원 손실 대비 KL 항의 검증용 비중. 실수 스칼라. 이 helper는 두 항을 각각 모든 원소 평균하므로 교재의 잠재 차원 합 ELBO와 스케일이 다르다.</td>
</tr>
</tbody></table></div>
<p><strong>반환값</strong></p>
<p>(total_loss,reconstruction_loss,kl)의 scalar Tensor 3개. total_loss.backward() 대상으로 첫 항목을 사용.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-9">복사</button></div><pre id="template-code-08-9" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 9"><code class="language-python">import hdat_templates as h
import torch
x = torch.tensor([[0., 1.], [2., 3.]])
model = h.VariationalAutoencoder(input_dim=x.shape[1], latent_dim=1, hidden_dim=8)
reconstruction, mu, logvar = model(x)
total, reconstruction_loss, kl = h.vae_loss(reconstruction, x, mu, logvar, beta=1.0)
total.backward()
assert total.ndim == reconstruction_loss.ndim == kl.ndim == 0
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>reconstruction/x는 같은 (B,F), mu/logvar는 같은 (B,Z), 모두 같은 장치의 실수 Tensor.</li>
<li>복원 MSE도 KL도 모든 원소에 대한 평균이다. KL을 Z축 합하고 B축 평균하는 교재식과 beta의 상대 스케일이 다르다.</li>
<li>반환 tuple 전체를 backward하거나 공통 loss_fn으로 그대로 전달하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-MLPGenerator"><code>MLPGenerator</code></h4>
<p>잡음 벡터에서 표형/평탄화 샘플을 생성한다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>noise_dim</code></td>
<td>필수</td>
<td><code>16</code></td>
<td>생성기에 입력할 표준정규 잡음 벡터의 폭. 양의 정수 Z. z=torch.randn(B, noise_dim)의 마지막 축.</td>
</tr>
<tr>
<td><code>output_dim</code></td>
<td>필수</td>
<td><code>X_train_flat.shape[1]</code></td>
<td>생성기가 만들 실제 샘플 한 개의 특성 수. 양의 정수 F. 원자료·Discriminator input_dim과 맞춰야 한다.</td>
</tr>
<tr>
<td><code>hidden_dim</code></td>
<td><code>128</code></td>
<td><code>64</code></td>
<td>문제 명세 또는 작은 검증으로 고른 내부 은닉 폭. 양의 정수. Discriminator는 hidden_dim//2 층을 쓰므로 최소 2 권장.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>generator(z)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>z</code></td>
<td>필수</td>
<td><code>torch.randn(4,3)</code></td>
<td>torch.randn(batch_size,noise_dim)로 만든 잡음. float32 Tensor (B,noise_dim), 모델과 같은 장치의 무작위 잡음.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: MLPGenerator 인스턴스. G(z): (B,output_dim) 실수 Tensor.</p>
<p><strong>반환값</strong></p>
<p>생성: MLPGenerator 인스턴스. G(z): (B,output_dim) 실수 Tensor.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-10">복사</button></div><pre id="template-code-08-10" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 10"><code class="language-python">import hdat_templates as h
import numpy as np
import torch
real = np.zeros((4, 6), dtype=np.float32)
noise_dim = 3
generator = h.MLPGenerator(noise_dim=noise_dim, output_dim=real.shape[1], hidden_dim=8)
z = torch.randn(len(real), noise_dim)
fake = generator(z)
assert fake.shape == (4, 6)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>noise_dim은 설계한 잡음 폭이고 output_dim은 실제 한 샘플의 특성 수다.</li>
<li>마지막 활성함수가 없으므로 출력 범위를 0..1로 제한하지 않는다. 실제 자료 전처리와 생성 범위를 맞춰야 한다.</li>
<li>GAN 교대 학습의 G/D 목적과 detach는 별도 루프로 작성한다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
<h4 id="api-MLPDiscriminator"><code>MLPDiscriminator</code></h4>
<p>한 샘플이 진짜인지 가짜인지 판별할 로짓을 만든다.</p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>input_dim</code></td>
<td>필수</td>
<td><code>X_train_flat.shape[1]</code></td>
<td>평탄화·전처리한 한 샘플의 입력 특성 수. 양의 정수 F. AE/VAE/Discriminator는 기본 (B,F) 입력이며 자동 flatten하지 않는다.</td>
</tr>
<tr>
<td><code>hidden_dim</code></td>
<td><code>128</code></td>
<td><code>64</code></td>
<td>문제 명세 또는 작은 검증으로 고른 내부 은닉 폭. 양의 정수. Discriminator는 hidden_dim//2 층을 쓰므로 최소 2 권장.</td>
</tr>
</tbody></table></div>
<p><strong>forward 호출 인자</strong> — <code>model(x)</code></p>
<div class="table-scroll" tabindex="0" role="region" aria-label="인자와 실제 값 비교 표"><table>
<thead>
<tr>
<th>인자</th>
<th>필수 여부·기본값</th>
<th>실제 값 예</th>
<th>값의 출처·shape·dtype·조건</th>
</tr>
</thead>
<tbody><tr>
<td><code>x</code></td>
<td>필수</td>
<td><code>xb</code></td>
<td>loader가 반환한 현재 배치의 입력 Tensor. float32 Tensor (B,input_dim), 실제 자료 또는 생성기 출력.</td>
</tr>
</tbody></table></div>
<p>반환: 생성: MLPDiscriminator 인스턴스. D(xb): (B,1) raw logit Tensor.</p>
<p><strong>반환값</strong></p>
<p>생성: MLPDiscriminator 인스턴스. D(xb): (B,1) raw logit Tensor.</p>
<p><strong>짧은 호출 예제</strong></p>
<p>아래 import부터 독립 실행합니다. hdat_templates.py를 같은 폴더에 두세요. 입력값은 예제 안에서 직접 만듭니다. PyTorch가 설치되어 있어야 이 API가 정의된다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 준비 코드와 실행 순서 확인</span><button type="button" data-copy-target="template-code-08-11">복사</button></div><pre id="template-code-08-11" tabindex="0" aria-label="모델을 만드는 인자 사전 코드 11"><code class="language-python">import hdat_templates as h
import numpy as np
import torch
real = np.zeros((4, 6), dtype=np.float32)
discriminator = h.MLPDiscriminator(input_dim=real.shape[1], hidden_dim=8)
logits = discriminator(torch.as_tensor(real))
assert logits.shape == (4, 1)
</code></pre></div>
<p><strong>주의할 점</strong></p>
<ul>
<li>input_dim은 실제 자료와 G 출력의 폭 F이다. 이진 판정이므로 input_dim=1이 되는 것은 아니다.</li>
<li>BCEWithLogitsLoss 앞에서 sigmoid를 추가하지 않는다.</li>
<li>hidden_dim//2 층이 있으므로 hidden_dim은 2 이상을 사용한다. 공통 지도학습 루프는 GAN의 교대 갱신을 대신하지 않는다.</li>
</ul>
<p>구현 확인: <a href="/hdat-ds-study-hub/downloads/hdat_templates.py">hdat_templates.py</a>.</p>
1:[["$","$La",null,{"active":"playbook"}],["$","div",null,{"className":"playbook-layout shell-wide","children":[["$","aside",null,{"className":"playbook-sidebar","children":[["$","a",null,{"className":"sidebar-back","href":"/hdat-ds-study-hub/templates/","children":"← 템플릿 호출 매뉴얼"}],["$","nav",null,{"aria-label":"호출 매뉴얼 장 목록","children":[["$","a","01",{"href":"/hdat-ds-study-hub/templates/01/","aria-current":"$undefined","children":[["$","span",null,{"children":"01"}],"함수의 괄호를 읽는 법부터"]}],["$","a","02",{"href":"/hdat-ds-study-hub/templates/02/","aria-current":"$undefined","children":[["$","span",null,{"children":"02"}],"표 데이터 하나로 처음부터 끝까지 호출하기"]}],["$","a","03",{"href":"/hdat-ds-study-hub/templates/03/","aria-current":"$undefined","children":[["$","span",null,{"children":"03"}],"회귀를 분류로 바꿀 때 함께 바꿀 것"]}],["$","a","04",{"href":"/hdat-ds-study-hub/templates/04/","aria-current":"$undefined","children":[["$","span",null,{"children":"04"}],"시계열에서 T와 F를 실제 값으로 바꾸기"]}],["$","a","05",{"href":"/hdat-ds-study-hub/templates/05/","aria-current":"$undefined","children":[["$","span",null,{"children":"05"}],"이미지와 오토인코더는 어디가 다른가요?"]}],["$","a","06",{"href":"/hdat-ds-study-hub/templates/06/","aria-current":"$undefined","children":[["$","span",null,{"children":"06"}],"지표·손실·저장 인자를 내 문제에 맞추기"]}],["$","a","07",{"href":"/hdat-ds-study-hub/templates/07/","aria-current":"$undefined","children":[["$","span",null,{"children":"07"}],"입력·전처리·분할·저장 함수 사전"]}],["$","a","08",{"href":"/hdat-ds-study-hub/templates/08/","aria-current":"page","children":[["$","span",null,{"children":"08"}],"모델을 만드는 인자 사전"]}],["$","a","09",{"href":"/hdat-ds-study-hub/templates/09/","aria-current":"$undefined","children":[["$","span",null,{"children":"09"}],"loader·학습·예측의 모든 인자"]}]]}]]}],["$","main",null,{"id":"main-content","className":"playbook-article","data-playbook":true,"children":[["$","header",null,{"className":"lesson-header","children":[["$","p",null,{"className":"micro-label","children":["hdat_templates · 호출 매뉴얼 ","08"]}],["$","h1",null,{"children":"모델을 만드는 인자 사전"}],["$","p",null,{"className":"playbook-note","children":"정의 → 값의 출처 → 호출 → 반환값 → 다음 단계"}]]}],["$","details",null,{"className":"playbook-mobile-list","children":[["$","summary",null,{"children":"매뉴얼의 다른 장 고르기"}],["$","nav",null,{"children":[["$","a","01",{"href":"/hdat-ds-study-hub/templates/01/","aria-current":"$undefined","children":["01",". ","함수의 괄호를 읽는 법부터"]}],["$","a","02",{"href":"/hdat-ds-study-hub/templates/02/","aria-current":"$undefined","children":["02",". ","표 데이터 하나로 처음부터 끝까지 호출하기"]}],["$","a","03",{"href":"/hdat-ds-study-hub/templates/03/","aria-current":"$undefined","children":["03",". ","회귀를 분류로 바꿀 때 함께 바꿀 것"]}],["$","a","04",{"href":"/hdat-ds-study-hub/templates/04/","aria-current":"$undefined","children":["04",". ","시계열에서 T와 F를 실제 값으로 바꾸기"]}],["$","a","05",{"href":"/hdat-ds-study-hub/templates/05/","aria-current":"$undefined","children":["05",". ","이미지와 오토인코더는 어디가 다른가요?"]}],["$","a","06",{"href":"/hdat-ds-study-hub/templates/06/","aria-current":"$undefined","children":["06",". ","지표·손실·저장 인자를 내 문제에 맞추기"]}],["$","a","07",{"href":"/hdat-ds-study-hub/templates/07/","aria-current":"$undefined","children":["07",". ","입력·전처리·분할·저장 함수 사전"]}],["$","a","08",{"href":"/hdat-ds-study-hub/templates/08/","aria-current":"page","children":["08",". ","모델을 만드는 인자 사전"]}],["$","a","09",{"href":"/hdat-ds-study-hub/templates/09/","aria-current":"$undefined","children":["09",". ","loader·학습·예측의 모든 인자"]}]]}]]}],["$","nav",null,{"className":"playbook-section-toc","aria-label":"현재 장 목차","children":[["$","a","api-MLP",{"href":"#api-MLP","children":"MLP"}],["$","a","api-CNN1D",{"href":"#api-CNN1D","children":"CNN1D"}],["$","a","api-SequenceRNN",{"href":"#api-SequenceRNN","children":"SequenceRNN"}],["$","a","api-SmallImageCNN",{"href":"#api-SmallImageCNN","children":"SmallImageCNN"}],["$","a","api-ResidualBlock2D",{"href":"#api-ResidualBlock2D","children":"ResidualBlock2D"}],["$","a","api-TransformerSequenceModel",{"href":"#api-TransformerSequenceModel","children":"TransformerSequenceModel"}],["$","a","api-Autoencoder",{"href":"#api-Autoencoder","children":"Autoencoder"}],["$","a","api-VariationalAutoencoder",{"href":"#api-VariationalAutoencoder","children":"VariationalAutoencoder"}],["$","a","api-vae_loss",{"href":"#api-vae_loss","children":"vae_loss"}],["$","a","api-MLPGenerator",{"href":"#api-MLPGenerator","children":"MLPGenerator"}],["$","a","api-MLPDiscriminator",{"href":"#api-MLPDiscriminator","children":"MLPDiscriminator"}]]}],["$","$Lb",null,{}],["$","$Lc",null,{"recipe":"template-08"}],false,["$","div",null,{"className":"lesson-content playbook-content template-content","dangerouslySetInnerHTML":{"__html":"$d"}}],"$Le","$Lf"]}]]}],"$L10"]
e:["$","div",null,{"className":"playbook-bottom","children":[["$","a",null,{"href":"/hdat-ds-study-hub/templates/09/","children":"학습 함수의 모든 인자"}],["$","a",null,{"href":"/hdat-ds-study-hub/playbook/","children":"문제 유형별 풀이"}],["$","a",null,{"href":"/hdat-ds-study-hub/downloads/hdat-template-guide.html","download":true,"children":"오프라인 매뉴얼 ↓"}]]}]
f:["$","nav",null,{"className":"lesson-pager","aria-label":"호출 매뉴얼 앞뒤 이동","children":[["$","a",null,{"href":"/hdat-ds-study-hub/templates/07/","children":["← ","입력·전처리·분할·저장 함수 사전"]}],["$","a",null,{"href":"/hdat-ds-study-hub/templates/09/","children":["loader·학습·예측의 모든 인자"," →"]}]]}]
10:["$","footer",null,{"className":"site-footer","children":[["$","div",null,{"className":"shell footer-grid","children":[["$","div",null,{"children":[["$","strong",null,{"children":"HDAT DS · PyTorch Study Room"}],["$","p",null,{"children":"Python을 처음 배우는 사람도 데이터 분석부터 차근차근 공부할 수 있도록 만든 개인 학습 자료입니다."}]]}],["$","nav",null,{"aria-label":"하단 메뉴","children":[["$","a",null,{"href":"/hdat-ds-study-hub/start/","children":"Python 기초 10강"}],["$","a",null,{"href":"/hdat-ds-study-hub/learn/","children":"HDAT 본과정 34강"}],["$","a",null,{"href":"/hdat-ds-study-hub/practice/","children":"실전 연습"}],["$","a",null,{"href":"/hdat-ds-study-hub/resources/","children":"자료실"}],["$","a",null,{"href":"/hdat-ds-study-hub/cheatsheet.html","children":"로컬 치트시트"}]]}]]}],["$","p",null,{"className":"legal shell","children":"이 사이트는 개인 학습용 비공식 자료이며 현대자동차그룹 또는 현대엔지비의 공식 사이트가 아닙니다. 시험 당일 문제 지시, 제공 스켈레톤, 공식 규정과 감독관 안내가 항상 우선합니다."}]]}]
