2:I["8c0f216c4604",[],"Children",1]
3:I["593f344dc510",[],"GlobalErrorBoundary",1]
4:I["0b874ad30386",[],"default",1]
5:I["593f344dc510",[],"ErrorBoundary",1]
6:I["15c18cfaeeff",[],"LayoutSegmentProvider",1]
7:I["8c0f216c4604",[],"Slot",1]
8:I["9276801271d6",[],"AppRouterScrollTarget",1]
9:I["593f344dc510",[],"RedirectBoundary",1]
:HL["/hdat-ds-study-hub/_next/static/css/index.COtfHe_8.css","style"     ]
0:{"__route":"route:/supplement/03","__interceptionContext":null,"__layoutIds":["layout:/"],"__rootLayout":"/","__sourcePage":"/supplement/[topic]/page","page:/supplement/03":"$L1","layout:/":[[[["$","link","css:/hdat-ds-study-hub/_next/static/css/index.COtfHe_8.css",{"rel":"stylesheet","precedence":"vite-rsc/importer-resources","href":"/hdat-ds-study-hub/_next/static/css/index.COtfHe_8.css","data-rsc-css-href":"/hdat-ds-study-hub/_next/static/css/index.COtfHe_8.css"}],"$undefined"],["$","html",null,{"lang":"ko","children":[["$","head",null,{"children":[["$","meta",null,{"name":"theme-color","content":"#07152b"}],["$","meta",null,{"name":"description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"name":"application-name","content":"HDAT DS Study Hub"}],["$","meta",null,{"name":"author","content":"HDAT DS Study Hub"}],["$","meta",null,{"name":"robots","content":"index, follow"}],["$","meta",null,{"property":"og:type","content":"website"}],["$","meta",null,{"property":"og:locale","content":"ko_KR"}],["$","meta",null,{"property":"og:url","content":"https://markshincuhk.github.io/hdat-ds-study-hub"}],["$","meta",null,{"property":"og:site_name","content":"HDAT DS Study Hub"}],["$","meta",null,{"property":"og:title","content":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}],["$","meta",null,{"property":"og:description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"property":"og:image","content":"https://markshincuhk.github.io/hdat-ds-study-hub/og.png"}],["$","meta",null,{"property":"og:image:width","content":"1726"}],["$","meta",null,{"property":"og:image:height","content":"911"}],["$","meta",null,{"property":"og:image:alt","content":"Python 기초부터 HDAT DS까지 배우는 PyTorch 학습실"}],["$","meta",null,{"name":"twitter:card","content":"summary_large_image"}],["$","meta",null,{"name":"twitter:title","content":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}],["$","meta",null,{"name":"twitter:description","content":"Python이라는 이름만 아는 사람을 위한 입문 10강부터 HDAT-DS 본과정 34강, 필기·실기 모의와 PyTorch 치트시트까지 이어지는 개인 학습실"}],["$","meta",null,{"name":"twitter:image","content":"https://markshincuhk.github.io/hdat-ds-study-hub/og.png"}],["$","link",null,{"rel":"icon","href":"/hdat-ds-study-hub/favicon.svg","type":"image/svg+xml"}],["$","title",null,{"children":"Python 기초부터 HDAT-DS까지 | PyTorch 학습실"}]]}],["$","body",null,{"children":["$","$L2",null,{}]}]]}]],null],"route:/supplement/03":[[["$","meta",null,{"charSet":"utf-8"}],[["$","title","0",{"children":"SVD·PCA·고전 ML·군집 실습 | HDAT DS"}]],[["$","meta","0",{"name":"viewport","content":"width=device-width, initial-scale=1"}]]],["$","$L3",null,{"fallback":"$4","children":["$","$L5",null,{"fallback":"$4","children":["$","$L6",null,{"providerId":"layout:/","segmentMap":{"children":["supplement","03"]},"children":["$","$L7",null,{"id":"layout:/","parallelSlots":"$undefined","children":["$","$L8",null,{"children":["$","$L9",null,{"children":[["$","$L6",null,{"providerId":"page:/supplement/03","segmentMap":{"children":["__PAGE__"]},"children":["$","$L7",null,{"id":"page:/supplement/03"}]}],null]}]}]}]}]}]}],null,null],"__layoutFlags":{"layout:/":"s"},"__artifactCompatibility":{"schemaVersion":1,"graphVersion":"app-route-graph:2bb3a25b1b286e10","deploymentVersion":"b09e4206-5d79-45c5-90be-eef7c466cf6b","appElementsSchemaVersion":1,"rscPayloadSchemaVersion":1,"rootBoundaryId":"/","renderEpoch":null},"__renderObservation":{"schemaVersion":1,"output":{"kind":"app-rsc","mountedSlotsFingerprint":null,"renderEpoch":null,"rootBoundaryId":"/","routeId":"route:/supplement/03"},"completeness":"partial","boundaryOutcome":{"kind":"unknown"},"requestApis":[{"kind":"connection","status":"unknown"},{"kind":"cookies","status":"unknown"},{"kind":"draftMode","status":"unknown"},{"kind":"headers","status":"unknown"},{"kind":"params","status":"observed"},{"kind":"searchParams","status":"unknown"}],"dynamicFetches":[],"cacheTags":["/supplement/03","_N_T_/layout","_N_T_/supplement/03","_N_T_/supplement/[topic]/layout","_N_T_/supplement/[topic]/page","_N_T_/supplement/layout"],"pathTags":["/supplement/03"],"cacheability":"unknown","downgrade":{"target":"freshRender","reasons":[{"code":"CP_DOWNGRADE_CACHEABILITY_UNKNOWN","target":"freshRender"},{"code":"CP_DOWNGRADE_INCOMPLETE_OBSERVATION","completeness":"partial","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"connection","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"cookies","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"draftMode","target":"freshRender"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"headers","target":"freshRender"},{"code":"CP_DOWNGRADE_PUBLIC_REQUEST_API","requestApi":"params","target":"publicVariant"},{"code":"CP_DOWNGRADE_UNKNOWN_REQUEST_API","requestApi":"searchParams","target":"freshRender"}],"fallback":{"kind":"breakerFallback","code":"CP_PRIVATE_DYNAMIC_DOWNGRADE","mode":"renderFresh","scope":"affectedOutput","fields":{"reasonCodes":["CP_DOWNGRADE_CACHEABILITY_UNKNOWN","CP_DOWNGRADE_INCOMPLETE_OBSERVATION","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API","CP_DOWNGRADE_PUBLIC_REQUEST_API","CP_DOWNGRADE_UNKNOWN_REQUEST_API"],"target":"freshRender"}},"isPublicCacheCandidate":false}}}
a:I["326fe58ccdc4",[],"default",1]
b:I["66e71be26559",[],"default",1]
c:I["ad205a1e87e8",[],"default",1]
d:T53f2,<h2 id="supplement-03-1">PCA를 SVD와 역변환까지 연결하기</h2>
<p>9강에서 새 축으로 좌표를 줄였다면, 이번에는 그 좌표로 원래 값이 얼마나 복원되는지 확인한다. [1,1], [2,2], [3,3]처럼 한 직선에 있는 점은 그 직선에서의 위치 하나로 복원할 수 있다. 직선 밖의 흔들림까지 있는 점에서 축 하나를 버리면 그 흔들림은 복원되지 않는다. <strong>역변환(inverse transform)</strong>은 남긴 좌표를 원래 공간으로 되돌리는 계산이지 버린 정보를 되찾는 마법이 아니다.</p>
<p>SVD는 원래 행렬을 세 행렬의 곱으로 나누어 표현하는 특이값 분해다. 여기서는 복잡한 행렬의 계산을 외우기보다 어느 행렬이 ‘새 방향’, 어느 숫자가 ‘그 방향의 크기’를 나타내는지 따라간다. N은 사례 수, F는 입력 특성 수, k는 남길 축 수다. 평균을 뺀 Xc에서 축을 골라 Z를 만든 뒤 같은 축을 이용해 복원하고 학습 평균을 다시 더한다. 아래 모양 표기는 그 단계들에서 행렬곱이 가능한지 확인하는 용도다.</p>
<p>선형대수와 차원 축소 개념을 보강하는 내용이다. 4강의 행렬곱, 9강의 PCA/LDA, 12강의 군집화를 먼저 공부한다. 이미지 생성과 대표 표본의 라벨을 전파하는 실험은 교육자료에 기반한 선택 심화 과정이다. PCA가 무엇을 보존하고 버리는지 직접 확인하면 분산 보존률을 예측 정확도로 오해하지 않게 된다.</p>
<p>중심화한 데이터 행렬 <code>Xc:[N,F]</code>의 축소형 SVD는 <code>Xc=UΣVᵀ</code>다. <code>r=min(N,F)</code>로 두면 <code>U:[N,r]</code>, <code>Σ:[r,r]</code>, <code>Vᵀ:[r,F]</code>이며 특이값은 음수가 아니다. 행렬의 계수(rank)가 작으면 특이값 일부는 0이다. <code>U</code>는 <code>XcXcᵀ</code>, <code>V</code>는 <code>XcᵀXc</code>의 고유벡터와 연결된다. 임의 정방행렬의 SVD가 그 행렬의 고유분해와 같다는 주장은 틀리다.</p>
<p>상위 k개의 오른쪽 특이벡터를 <code>V_k:[F,k]</code>로 모으면 좌표는 <code>Z=XcV_k</code>, 복원은 <code>X_hat=ZV_kᵀ+mean_train</code>이다. 표본 공분산의 주성분 분산은 <code>singular_value²/(N−1)</code>이며 N&gt;1을 가정한다. PCA 좌표계에서 k-means 중심을 고른 뒤 역변환하면 원래 공간의 대표 패턴을 볼 수 있다. 이것은 학습한 대표점을 복원하는 실험이며, 새로운 사람의 이미지나 다양한 실제 사례를 생성한다고 보장하지는 않는다.</p>
<p>손계산: 중심화된 행렬 <code>[[−1,0],[1,0]]</code>의 첫 방향은 첫 축, 특이값은 <code>sqrt(2)</code>다. 표본분산은 2, 둘째 방향 분산은 0이다. k=1로 줄여 복원해도 정보 손실이 없다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 이 블록 전체를 새 셀에서 실행</span><button type="button" data-copy-target="supplement-code-03-1">복사</button></div><pre id="supplement-code-03-1" tabindex="0" aria-label="SVD·PCA·고전 ML·군집 실습 코드 1"><code class="language-python">import numpy as np

# 이미 train/valid로 구분된 독립 합성 데이터
train = np.array([[-2., 0.], [-1., 0.], [1., 0.], [2., 0.]])
valid = np.array([[3., 1.], [-3., -1.]])
mean = train.mean(axis=0)
centered = train - mean
u, singular, vt = np.linalg.svd(centered, full_matrices=False)
components = vt[:1]                    # [k,F] = [1,2]
z_train = centered @ components.T      # [N,k]
z_valid = (valid - mean) @ components.T
recon_train = z_train @ components + mean
recon_valid = z_valid @ components + mean
variance = singular**2 / (len(train) - 1)
assert np.allclose(recon_train, train)
assert z_valid.shape == (2, 1)
assert np.isclose(np.square(recon_valid - valid).mean(), 0.5)
assert np.isclose(variance.sum(), np.var(train, axis=0, ddof=1).sum())
print("PCA 복원 검사 통과", "valid MSE:", 0.5)
</code></pre></div>
<p>직접 풀 문제: 훈련 데이터에서 스케일러와 PCA를 학습하고 검증 데이터에는 변환만 적용하는 이유는? k를 늘릴수록 훈련 데이터의 복원 오차가 줄어도 이를 활용하는 후속 분류 성능이 반드시 높아지지는 않는 이유는? 위 예제에서 검증 데이터의 두 번째 좌표에 이상탐지에 필요한 정보가 있다면 k=1로 줄일 때 무엇을 잃는가?</p>
<details class="answer-panel"><summary>해설 보기</summary><p>검증 데이터의 분포로 축을 선택하면 모델 비교에 쓸 데이터가 전처리 학습에 섞인다. 분산 보존과 정답 예측은 목적이 다르므로 분산이 작은 방향도 예측에 중요할 수 있다. 예제의 k=1 투영은 두 번째 좌표를 전부 버리므로 그 좌표에 있는 구분 정보도 사라진다. 다만 원래 공간에서의 복원 오차를 이상 점수(anomaly score)로 쓰는 별도 설계에서는 버려진 방향의 오차를 측정할 수 있다.</p>
</details><h2 id="supplement-03-2">라벨을 붙일 수 있는 표본 수가 제한된 경우의 군집 대표 표본</h2>
<p>교육자료에 기반한 선택 심화 내용이며 공식 DS 필수 실기 항목은 아니다.</p>
<p>라벨이 일부에만 있는 경우 훈련 특성을 군집화한 뒤 각 중심에 가장 가까운 표본을 골라 사람이 라벨을 붙이는 실험을 할 수 있다. 같은 수의 표본을 무작위로 골라 라벨을 붙인 경우와 비교한다. 전체 훈련 정답을 사용한 모델은 라벨 수가 다르므로 같은 조건의 비교 대상이 아니라 참고용 상한으로 따로 표시한다.</p>
<p>대표 표본의 라벨을 같은 군집 전체에 전파하면 학습 표본이 늘지만, 군집 안에 여러 클래스가 섞여 있으면 잘못된 정답도 늘어난다. 중심에 가까운 표본이 항상 그 군집의 정답 라벨을 대표하는 것은 아니다. 대표점에서 가까운 표본에만 먼저 라벨을 전파하고 신뢰도에 따른 성능을 비교할 수 있다. 검증·테스트 라벨은 표본 선택이나 라벨 전파에 사용하지 않는다.</p>
<p>실습 조건: 훈련 데이터를 30개 군집으로 나누고 대표점 30개의 라벨만 공개한다. 무작위 표본 30개로 학습한 모델, 대표점 30개로 학습한 모델, 거리를 제한해 라벨을 전파한 모델을 같은 독립 검증 데이터에서 비교한다. 선택한 대표점이 중복되면 실제로 라벨을 확인한 고유 표본 수도 기록한다. 실루엣 점수(silhouette)는 군집이 기하학적으로 얼마나 분리되었는지 나타내며 라벨 예측 정확도를 대신하지 않는다.</p>
<h2 id="supplement-03-3">고전 ML을 같은 검증 조건에서 직접 비교하기</h2>
<p>이 절은 교육자료를 확장한 실습이다. scikit-learn으로 고전 머신러닝 모델을 비교하고, 신경망은 계속 PyTorch로 구현한다. 특정 모델이나 아래 실습 전체가 공식 필수 구현이라는 뜻은 아니다. 본강 8~13강의 스케일링, 특성 선택, PCA/LDA, 앙상블, 검증을 먼저 공부한다.</p>
<p>모델 이름을 아는 것과 새 데이터에서 모델들을 공정하게 비교하는 것은 다르다. 이 실습에서는 원자료의 분할을 먼저 고정한 뒤 전처리와 모델을 하나의 Pipeline으로 묶는다. 교차검증(CV)은 각 폴드(fold)에서 Pipeline을 새로 학습하므로 평균·표준편차·PCA 축·LDA 축·선택할 특성을 검증 폴드에서 배우지 않는다.</p>
<p>이 합성 데이터는 각 행이 독립적인 표형 데이터라고 가정한다. 실제 데이터가 같은 차량의 반복 관측이거나 시간순 자료라면 아래의 무작위 층화 분할을 그대로 적용하지 말고 그룹 또는 시간순 분할을 설계한다.</p>
<h3>데이터 구성과 모델의 입력·출력 조건</h3>
<p>데이터는 540행이며 수치 특성 12개와 클래스 3개로 구성된다. 먼저 최종 테스트 108행을 따로 두고, 나머지에서 검증 108행을 분리한다. 훈련 324행 내부에서 같은 3개 폴드로 교차검증하여 후보를 비교한다. 이 실습의 검증 데이터는 선택한 모델을 독립적으로 점검하는 용도이며 다시 후보를 고르는 데 사용하지 않는다. 최종 선택을 확정하면 훈련·검증 데이터를 합쳐 재학습하고 테스트를 한 번만 평가한다. 실제 작업에서 검증 결과를 보고 다시 수정했다면 그 검증 점수는 더 이상 최종 일반화 성능의 독립적인 증거가 아니다.</p>
<p>PCA/LDA는 원래 특성을 새 좌표로 바꾸고, SelectFromModel은 원래 특성 중 일부만 남긴다. 클래스가 3개이면 LDA가 만드는 축은 최대 2개다. 아래 특성 선택에 쓰는 랜덤 포레스트(Random Forest)는 특성 중요도를 얻기 위한 고전 머신러닝 모델이며 별도의 신경망을 사용하지 않는다.</p>
<h3>독립 실행 예제</h3>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 이 블록 전체를 새 셀에서 실행</span><button type="button" data-copy-target="supplement-code-03-2">복사</button></div><pre id="supplement-code-03-2" tabindex="0" aria-label="SVD·PCA·고전 ML·군집 실습 코드 2"><code class="language-python">import time
import numpy as np
from sklearn.base import clone
from sklearn.datasets import make_classification
from sklearn.model_selection import train_test_split, StratifiedKFold, cross_val_score
from sklearn.pipeline import make_pipeline
from sklearn.preprocessing import StandardScaler
from sklearn.decomposition import PCA
from sklearn.discriminant_analysis import LinearDiscriminantAnalysis
from sklearn.feature_selection import SelectFromModel
from sklearn.neighbors import KNeighborsClassifier
from sklearn.svm import SVC
from sklearn.tree import DecisionTreeClassifier
from sklearn.ensemble import RandomForestClassifier, BaggingClassifier, VotingClassifier
from sklearn.metrics import f1_score

started = time.perf_counter()
X, y = make_classification(
    n_samples=540, n_features=12, n_informative=6, n_redundant=2,
    n_classes=3, n_clusters_per_class=1, weights=[0.55, 0.30, 0.15],
    class_sep=1.0, random_state=23,
)
# 인위적으로 단위를 다르게 하되 target은 전처리에 사용하지 않는다.
X *= np.logspace(0, 2, X.shape[1])
all_ids = np.arange(len(y))
dev_ids, test_ids = train_test_split(
    all_ids, test_size=0.2, stratify=y, random_state=23,
)
train_ids, valid_ids = train_test_split(
    dev_ids, test_size=0.25, stratify=y[dev_ids], random_state=24,
)
assert not (set(train_ids) &amp; set(valid_ids))
assert not (set(dev_ids) &amp; set(test_ids))
assert (len(train_ids), len(valid_ids), len(test_ids)) == (324, 108, 108)

knn = make_pipeline(StandardScaler(), KNeighborsClassifier(n_neighbors=7))
svm = make_pipeline(StandardScaler(), SVC(C=1.0, gamma="scale"))
models = {
    "scaled KNN": knn,
    "scaled SVM": svm,
    "PCA + KNN": make_pipeline(
        StandardScaler(), PCA(n_components=6), KNeighborsClassifier(7)),
    "LDA + KNN": make_pipeline(
        StandardScaler(), LinearDiscriminantAnalysis(n_components=2),
        KNeighborsClassifier(7)),
    "selected + KNN": make_pipeline(
        StandardScaler(),
        SelectFromModel(RandomForestClassifier(n_estimators=24, random_state=23),
                        threshold="median"),
        KNeighborsClassifier(7)),
    "bagged tree": BaggingClassifier(
        estimator=DecisionTreeClassifier(max_depth=5),
        n_estimators=8, random_state=23),
    "hard voting": VotingClassifier(
        estimators=[("knn", knn), ("svm", svm),
                    ("tree", DecisionTreeClassifier(max_depth=5, random_state=23))],
        voting="hard"),
}
folds = list(StratifiedKFold(n_splits=3, shuffle=True, random_state=25).split(
    X[train_ids], y[train_ids]))
means = {}
for name, model in models.items():
    scores = cross_val_score(model, X[train_ids], y[train_ids], cv=folds,
                             scoring="f1_macro", error_score="raise")
    assert scores.shape == (3,) and np.isfinite(scores).all()
    means[name] = scores.mean()
    print(f"{name:16s} CV mean={scores.mean():.3f}, std={scores.std():.3f}")

chosen = max(means, key=means.get)  # 선택에는 train 내부 CV만 사용
selected = clone(models[chosen]).fit(X[train_ids], y[train_ids])
valid_pred = selected.predict(X[valid_ids])
assert valid_pred.shape == y[valid_ids].shape
print("CV로 고정한 모델:", chosen)
print("valid Macro-F1:", f1_score(y[valid_ids], valid_pred, average="macro"))

# 여기까지 선택을 고정한 뒤 마지막 평가를 한 번 수행한다.
final_model = clone(models[chosen]).fit(X[dev_ids], y[dev_ids])
test_pred = final_model.predict(X[test_ids])
print("final test Macro-F1:", f1_score(y[test_ids], test_pred, average="macro"))
print(f"총 실행 시간: {time.perf_counter() - started:.2f}초")
</code></pre></div>
<p>실습을 변형할 때는 위 코드의 테스트 출력 부분을 잠시 빼 두고 훈련 데이터 내부의 교차검증이나 검증 데이터로만 결정한다. 마지막 테스트 점수를 이미 확인한 데이터를 계속 사용하면 ‘한 번만 평가’ 원칙을 지킬 수 없다. 추가 연습에서는 새 난수 시드(seed)로 테스트 데이터를 다시 만들고 최종 단계까지 보지 않는다.</p>
<h3>결과를 읽는 법</h3>
<p>교차검증 평균은 같은 분할에서 얻은 세 폴드 점수의 평균이다. 표준편차는 폴드 사이의 변동을 보여 주지만 세 폴드만으로 엄밀한 신뢰구간을 구할 수는 없다. 후보를 많이 비교해 가장 높은 점수를 고르면 그 최고점도 낙관적일 수 있다. 표준편차가 작다는 이유만으로 좋은 모델이라고 판정하지 않는다.</p>
<p>배깅(Bagging)은 같은 종류의 트리를 부트스트랩(bootstrap) 표본에서 여러 번 학습한다. 보팅(Voting)은 서로 다른 종류의 모델이 예측한 클래스를 투표로 합친다. 이 예제는 클래스 라벨을 사용하는 하드 보팅(hard voting)이므로 SVM의 확률 추정이 필요 없다. 확률을 사용하는 소프트 보팅(soft voting)으로 바꾸려면 모든 구성 모델의 확률 출력과 클래스별 열 순서를 확인해야 한다.</p>
<p>PCA가 분산을 많이 보존하거나 랜덤 포레스트가 중요하다고 판단한 특성을 남겼다고 해서 KNN 성능이 반드시 좋아지지는 않는다. PCA·LDA·특성 선택을 거치면 거리 계산에 쓰는 좌표와 남은 정보가 달라지므로 실제 평가지표로 어떤 변환이 도움이 되는지 비교한다. 합성 데이터 한 번의 비교에서 가장 좋았던 모델이 실제 자동차 데이터에서도 가장 좋다고 일반화하지 않는다.</p>
<h3>직접 바꾸어 풀기</h3>
<ol>
<li>PCA 차원을 6에서 3으로 바꿀 때 학습 시간이 줄고 성능이 달라지는 이유를 설명하라. LDA도 3으로 바꾸어도 되는가?</li>
<li>데이터 분할 전에 <code>SelectFromModel</code>을 전체 X,y로 학습한 뒤 이 코드를 실행하면 왜 누수인가?</li>
<li>KNN에서 StandardScaler를 제거한 후보를 추가하라. 단위가 100배 다른 특성이 거리 계산에 어떤 영향을 주는지 예측한 뒤 확인하라.</li>
<li>선택한 모델을 훈련·검증 데이터를 합쳐 재학습했다면 검증 점수를 다시 계산해 ‘최종 일반화 성능’이라고 부를 수 있는가?</li>
<li>보팅의 성능이 가장 좋지 않아도 코드가 틀렸다는 뜻은 아닌 이유를 설명하라.</li>
</ol>
<details class="answer-panel"><summary>해설 보기</summary><ol>
<li>PCA 출력이 <code>[N,3]</code>이 되어 거리 계산량은 줄지만 정답 예측에 유용한 방향도 버릴 수 있다. 3클래스 LDA의 일반적인 축 수 상한은 2이므로 3축 설정은 맞지 않는다.</li>
<li>검증·테스트 정답을 보고 특성을 선택했기 때문이다. 교차검증 폴드를 나누어도 이미 선택된 열에 정답 정보가 반영되어 낙관적인 평가가 된다.</li>
<li>유클리드 거리에서는 각 특성의 차이를 제곱한다. 단위만 100배 커져도 해당 항은 10,000배 커져 다른 특성보다 훨씬 큰 영향을 줄 수 있다. 실제 영향은 값의 분포와 정보량에도 달려 있다.</li>
<li>재학습에 검증 데이터가 들어갔으므로 그 점수는 훈련 성능의 일부다. 최종 평가는 아직 학습·선택에 쓰지 않은 테스트 데이터로 한다.</li>
<li>구성 모델들의 오류가 비슷하거나 약한 모델의 투표가 강한 모델의 올바른 예측을 바꾸면 성능이 개선되지 않을 수 있다. 앙상블을 사용한다고 성능 향상이 보장되지는 않는다.</li>
</ol>
</details><h2 id="supplement-03-4">군집 실행과 실루엣 점수를 계산할 수 없는 경우</h2>
<p>교육자료에 기반한 선택 실습이다. 군집화는 정답 라벨 없이 특성의 구조를 찾는다. DBSCAN의 잡음 라벨 −1을 실제 클래스로 해석하지 않으며, 잡음을 반드시 제거해야 하는 데이터 오류라고 단정하지도 않는다. 아래 코드는 같은 특성을 K-means와 DBSCAN으로 탐색하고 <strong>잡음을 제외한 군집의 실루엣 점수</strong>와 전체 잡음 비율을 따로 보고한다.</p>
<p>이 예제는 주어진 데이터 전체의 군집 구조를 살피는 탐색 분석이며, 새로운 테스트 데이터의 예측 성능을 평가하는 실험은 아니다. 군집에서 얻은 특성을 분류기 입력으로 쓰려면 앞 절처럼 훈련 폴드에서만 스케일러와 군집 모델을 학습해야 한다.</p>
<div class="code-shell"><div class="code-toolbar"><span>PYTHON · 이 블록 전체를 새 셀에서 실행</span><button type="button" data-copy-target="supplement-code-03-3">복사</button></div><pre id="supplement-code-03-3" tabindex="0" aria-label="SVD·PCA·고전 ML·군집 실습 코드 3"><code class="language-python">import numpy as np
from sklearn.datasets import make_moons
from sklearn.preprocessing import StandardScaler
from sklearn.cluster import KMeans, DBSCAN
from sklearn.metrics import silhouette_score

def summarize_clusters(X, labels):
    labels = np.asarray(labels)
    keep = labels != -1
    n = int(keep.sum())
    k = len(np.unique(labels[keep]))
    noise_ratio = float((~keep).mean())
    # silhouette는 평가 표본에 군집이 2개 이상, 표본수보다 적게 있어야 한다.
    score = silhouette_score(X[keep], labels[keep]) if 2 &lt;= k &lt; n else None
    return {"clusters": k, "noise_ratio": noise_ratio,
            "silhouette_without_noise": score}

X, _ = make_moons(n_samples=240, noise=0.06, random_state=31)
X = StandardScaler().fit_transform(X)
for name, model in (
    ("k-means", KMeans(n_clusters=2, n_init=10, random_state=31)),
    ("DBSCAN", DBSCAN(eps=0.30, min_samples=5)),
):
    report = summarize_clusters(X, model.fit_predict(X))
    assert 0 &lt;= report["noise_ratio"] &lt;= 1
    print(name, report)

# 전부 noise와 단일 군집에서는 점수를 만들지 않는 계약을 검사한다.
all_noise = DBSCAN(eps=0.01, min_samples=len(X)+1).fit_predict(X)
assert summarize_clusters(X, all_noise)["silhouette_without_noise"] is None
assert summarize_clusters(X, np.zeros(len(X), dtype=int))[
    "silhouette_without_noise"] is None
print("군집 퇴화 조건 검사 통과")
</code></pre></div>
<p>실루엣의 <code>a</code>는 같은 군집의 다른 표본까지 평균거리다. <code>b</code>는 다른 군집 각각까지 평균거리를 구한 뒤 그중 가장 작은 값이다. 다른 군집의 중심점까지 거리와는 다르다. 곡선형 군집은 눈으로 의미 있게 구분되어도 유클리드 거리로 계산한 실루엣 점수가 기대보다 낮을 수 있다. DBSCAN 후보마다 제외되는 잡음의 비율이 다르므로 이 점수만 비교하면 쉬운 표본만 남긴 후보를 고를 수 있다. 잡음 비율과 실제 분석 목적도 함께 고려한다.</p>
<p>직접 풀 문제: eps를 0.10과 0.60으로 바꾸어 군집 수와 잡음 비율을 관찰하라. 실루엣 점수가 가장 큰 eps를 무조건 고르면 안 되는 이유는? DBSCAN 결과가 모두 −1이면 −1을 하나의 일반 군집으로 간주해 점수를 계산해도 되는가?</p>
<details class="answer-panel"><summary>해설 보기</summary><p>eps가 작으면 이웃이 부족해 잡음이 늘기 쉽고, 크면 군집이 합쳐질 수 있다. 정확한 변화는 데이터에 따라 다르다. 후보마다 평가에 남는 표본이 다르거나 실제 의미와 맞지 않는 군집이 만들어질 수 있으므로 점수만 최적화하지 않는다. 모두 −1이면 이 실습의 정의상 군집에 배정된 표본이 없다. 실루엣의 정의 조건도 만족하지 않으므로 억지로 점수를 출력하지 않는다.</p>
</details><p>참고: <a href="https://scikit-learn.org/1.5/common_pitfalls.html">Pipeline과 전처리 누수 예방</a>, <a href="https://scikit-learn.org/1.5/modules/ensemble.html">공식 앙상블 안내</a>, <a href="https://scikit-learn.org/1.5/modules/generated/sklearn.decomposition.PCA.html">PCA 문서</a>. 교육자료 확장 실습이며 모델 선택 권고나 실기 출제 보장이 아닙니다.</p>
1:[["$","$La",null,{"active":"learn"}],["$","div",null,{"className":"playbook-layout shell-wide","children":[["$","aside",null,{"className":"playbook-sidebar","children":[["$","a",null,{"className":"sidebar-back","href":"/hdat-ds-study-hub/supplement/","children":"← 보충강의 목록"}],["$","nav",null,{"aria-label":"보충강의","children":[["$","a","01",{"href":"/hdat-ds-study-hub/supplement/01/","aria-current":"$undefined","children":["01",". ","다중입력·모델 결합·전용 학습루프"]}],["$","a","02",{"href":"/hdat-ds-study-hub/supplement/02/","aria-current":"$undefined","children":["02",". ","Seq2Seq의 학습 입력과 독립 추론"]}],["$","a","03",{"href":"/hdat-ds-study-hub/supplement/03/","aria-current":"page","children":["03",". ","SVD·PCA·고전 ML·군집 실습"]}],["$","a","04",{"href":"/hdat-ds-study-hub/supplement/04/","aria-current":"$undefined","children":["04",". ","베이즈 탐색·기대 개선량·실험 보고"]}]]}]]}],["$","main",null,{"id":"main-content","className":"playbook-article","data-playbook":true,"children":[["$","header",null,{"className":"lesson-header","children":[["$","p",null,{"className":"micro-label","children":["심화 보충강의 · ","03"]}],["$","h1",null,{"children":"SVD·PCA·고전 ML·군집 실습"}],["$","p",null,{"className":"lesson-summary","children":"교육자료 대조에서 부족했던 연결 설명과 독립 실습입니다. 본과정 이후 선택하는 보충강의이며, 모두 공식 필수 구현이라는 뜻은 아닙니다."}]]}],["$","a",null,{"className":"supplement-entry","href":"/hdat-ds-study-hub/review/","children":"공식 범위와 학습 순서 구분하기 →"}],["$","$Lb",null,{}],["$","nav",null,{"className":"playbook-section-toc","aria-label":"보충강의 목차","children":[["$","a","supplement-03-1",{"href":"#supplement-03-1","children":"PCA를 SVD와 역변환까지 연결하기"}],["$","a","supplement-03-2",{"href":"#supplement-03-2","children":"라벨을 붙일 수 있는 표본 수가 제한된 경우의 군집 대표 표본"}],["$","a","supplement-03-3",{"href":"#supplement-03-3","children":"고전 ML을 같은 검증 조건에서 직접 비교하기"}],["$","a","supplement-03-4",{"href":"#supplement-03-4","children":"군집 실행과 실루엣 점수를 계산할 수 없는 경우"}]]}],["$","$Lc",null,{"recipe":"supplement-03"}],["$","div",null,{"className":"lesson-content","dangerouslySetInnerHTML":{"__html":"$d"}}],"$Le","$Lf"]}]]}],"$L10"]
e:["$","section",null,{"className":"official-sources","children":[["$","h2",null,{"children":"이 강의 코드 내려받기"}],["$","p",null,{"children":"각 파일은 위의 한 코드 블록입니다. 주석과 설명의 실행 조건을 확인하세요. 직접 만든 예제 입력으로 동작을 확인한 코드이며, 실제 데이터의 예측 성능을 보장하지는 않습니다."}],["$","div",null,{"children":[["$","a","supplement-03-1.py",{"href":"/hdat-ds-study-hub/downloads/supplement-03-1.py","download":true,"children":[["$","strong",null,{"children":["예제 ",1," Python"]}],["$","span",null,{"children":"supplement-03-1.py"}]]}],["$","a","supplement-03-2.py",{"href":"/hdat-ds-study-hub/downloads/supplement-03-2.py","download":true,"children":[["$","strong",null,{"children":["예제 ",2," Python"]}],["$","span",null,{"children":"supplement-03-2.py"}]]}],["$","a","supplement-03-3.py",{"href":"/hdat-ds-study-hub/downloads/supplement-03-3.py","download":true,"children":[["$","strong",null,{"children":["예제 ",3," Python"]}],["$","span",null,{"children":"supplement-03-3.py"}]]}]]}]]}]
f:["$","nav",null,{"className":"lesson-pager","aria-label":"다음 학습","children":[["$","a",null,{"href":"/hdat-ds-study-hub/supplement/","children":"← 보충강의 고르기"}],["$","a",null,{"href":"/hdat-ds-study-hub/practice/labs/","children":"치트시트 실전 문제 풀기 →"}]]}]
10:["$","footer",null,{"className":"site-footer","children":[["$","div",null,{"className":"shell footer-grid","children":[["$","div",null,{"children":[["$","strong",null,{"children":"HDAT DS · PyTorch Study Room"}],["$","p",null,{"children":"Python을 처음 배우는 사람도 데이터 분석부터 차근차근 공부할 수 있도록 만든 개인 학습 자료입니다."}]]}],["$","nav",null,{"aria-label":"하단 메뉴","children":[["$","a",null,{"href":"/hdat-ds-study-hub/start/","children":"Python 기초 10강"}],["$","a",null,{"href":"/hdat-ds-study-hub/learn/","children":"HDAT 본과정 34강"}],["$","a",null,{"href":"/hdat-ds-study-hub/practice/","children":"실전 연습"}],["$","a",null,{"href":"/hdat-ds-study-hub/resources/","children":"자료실"}],["$","a",null,{"href":"/hdat-ds-study-hub/cheatsheet.html","children":"로컬 치트시트"}]]}]]}],["$","p",null,{"className":"legal shell","children":"이 사이트는 개인 학습용 비공식 자료이며 현대자동차그룹 또는 현대엔지비의 공식 사이트가 아닙니다. 시험 당일 문제 지시, 제공 스켈레톤, 공식 규정과 감독관 안내가 항상 우선합니다."}]]}]
